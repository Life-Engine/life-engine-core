# Change Log

## 2026-03-29

- Architecture Migration WP 10.5 — First-Party Plugin Migration: migrate all 10 first-party plugins to the new SDK architecture. Update all 8 existing manifest.toml files to use the new capabilities format (required = [...] array with colon-delimited capability tokens like "storage:doc:read") and remove unsupported method/path fields from action definitions. Create manifest.toml for api-caldav and api-carddav (previously missing, making them undiscoverable by the plugin loader). Add Plugin trait implementation and register_plugin! macro to webhook-receiver, api-caldav, and api-carddav (previously CorePlugin-only, unable to load as WASM plugins). Create error modules with EngineError impl for the 3 newly migrated plugins. Add crate-type = ["cdylib", "rlib"] to Cargo.toml for webhook-receiver, api-caldav, and api-carddav to enable WASM compilation target. Add thiserror dependency to the 3 newly migrated plugins. All 7 compilable plugin crates pass cargo check and clippy clean (3 crates have pre-existing ContactName struct errors in serializer/google modules unrelated to this migration).

- Architecture Migration WP 10.4 — Migration from Current Architecture: migrate legacy monolithic code from apps/core/src/ into the corresponding packages/ crates. Move StorageAdapter trait and types to packages/traits/src/legacy_storage.rs, legacy SqliteStorage implementation to packages/storage-sqlite/src/legacy.rs (including Argon2Settings and open_encrypted using package encryption module). Move domain-separated HKDF crypto (derive_key, encrypt/decrypt wrappers, HMAC, domain constants) to packages/crypto/src/domain.rs with new CryptoError::InvalidKeyLength variant. Move transport-agnostic auth providers (LocalToken, OIDC, JWT, WebAuthn) to packages/auth/src/legacy/ with public visibility for TokenState/StoredToken fields; fix RSA test key generation to use OsRng for CryptoRngCore compatibility. Move UI plugin manifest parser to packages/plugin-system/src/legacy_manifest.rs using PluginError::ManifestInvalid instead of CoreError::Manifest. Move MessageBus (BusEvent enum + broadcast pub/sub) to packages/workflow-engine/src/message_bus.rs using legacy_storage::Record. All apps/core modules become thin re-export shims (pub use from packages) preserving backward compatibility — no route handler or startup code changes needed. Update Cargo.toml for 5 packages (add anyhow, hex, argon2, rusqlite, webauthn-rs, rsa, rcgen dependencies as needed). All 7 modified packages compile clean; 425+ tests pass across packages (167 auth, 216 plugin-system, 48 crypto/traits/storage/workflow).

- Architecture Migration WP 10.3 — Default Configuration Generation: enhance the Phase 9 startup config system to generate working default configuration files on first run instead of commented-out stubs. When Core starts with no existing config.toml, write a fully active STARTER_CONFIG with SQLite document storage (data/core.db), filesystem blob storage (data/blobs), local-token auth, REST+GraphQL transport on localhost:8080, and standard plugin/workflow paths. Generate companion listeners.yaml (full REST+GraphQL listener definition with CRUD routes, health endpoint, and GraphQL endpoint) and storage.toml (SQLite document adapter with WAL mode, connection pool, busy timeout; filesystem blob adapter with max size and cleanup interval) in the config directory alongside config.toml. Existing files are never overwritten on subsequent starts. Add generate_default_configs() public function to startup module. 7 new tests: STARTER_CONFIG TOML parsing, starter config validation, companion file generation, skip-existing behavior, nested directory creation, full first-run integration, and subsequent-load non-overwrite. cargo check and clippy pass clean.

- Architecture Migration WP 10.2 — System Workflows: create 7 default system workflow YAML files in workflows/ directory (collection.list, collection.get, collection.create, collection.update, collection.delete, graphql.query, system.health). Each workflow defines a single step dispatching to the built-in system.crud plugin. Add SystemCrudHandler to packages/workflow-engine/src/system_steps.rs implementing PluginExecutor for the system.crud plugin — delegates CRUD operations to StorageContext with CallerIdentity::System (bypasses capability checks). Actions: list (doc_query with pagination), get (doc_get), create (doc_create), update (doc_update), delete (doc_delete), graphql_resolve (pass-through stub), health_check (storage reachability probe). Add CompositePluginExecutor that routes system.crud actions to SystemCrudHandler and all others to the underlying WASM plugin executor. Export SystemCrudHandler, CompositePluginExecutor, and SYSTEM_CRUD_PLUGIN_ID from workflow-engine crate. 4 new unit tests (param extraction from nested/top-level, plugin ID constant, payload round-trip). All 195 workflow-engine tests pass.

- Architecture Migration WP 10.1 — Core Binary Startup Orchestration: rewrite apps/core/src/main.rs startup sequence to initialise the new four-layer pipeline architecture alongside legacy subsystems. Create StorageRouter wrapping SqliteDocumentAdapter (second WAL-mode connection to the same database) and FsBlobAdapter with configurable timeouts and health checks. Build StorageContext enforcement layer with CDM schema registry (loaded via load_cdm_schemas) and audit event channel (background task drains and logs events). Replace manual PipelineExecutor + TriggerRegistry wiring with WorkflowEngine facade that initialises executor, trigger registry, EventBus, and Scheduler in a single call. Update graceful shutdown (shutdown.rs) to accept and drop WorkflowEngine during the workflow engine teardown step, cancelling scheduler tasks and releasing the event bus. Add workflow_engine field to ShutdownHandles. Build succeeds with no new warnings; all pre-existing tests unaffected.

- Architecture Migration WP 9.7 — Transport Equivalence Verification: add transport_equivalence.rs integration test to packages/transport-rest/tests/ proving REST and GraphQL produce identical results for the same workflow. 6 tests cover: equivalent WorkflowRequest construction (query params, identity, source binding), identical data payloads from the same WorkflowResponse, error information preservation across transports, HTTP-level equivalence (both routers return matching data envelopes), status code mapping consistency across all WorkflowStatus variants, and Created status success handling. Add life-engine-transport-graphql dev-dependency to transport-rest. All 6 equivalence tests pass.

- Architecture Migration WP 9.6 — Listener and TLS: add build_layered_router() function to listener.rs that assembles the full middleware stack (CatchPanic → CORS → Auth → Logging) onto an Axum Router, with automatic public route collection from ListenerConfig. Add catch-panic feature to tower-http workspace dependency. Add rcgen dev-dependency for TLS test cert generation. 8 new integration tests: public route auth bypass, missing token rejection, invalid token rejection, valid token acceptance, REST+GraphQL on same listener, panic handler catch, plain HTTP listener startup with reqwest client, and HTTPS/TLS listener startup with self-signed cert and custom trust store. All 68 transport-rest tests pass.

- Architecture Migration WP 9.4 — GraphQL Handler: update graphql_handler to accept Extension<Identity> from auth middleware instead of hardcoded Identity::guest(), build WorkflowRequest via translate_request, and return properly translated WorkflowResponse via into_graphql_response (placeholder dispatch until Phase 10). Implement build_dynamic_schema() in config.rs using async-graphql dynamic schema API — generates Object types from PluginSchemaDeclaration with query resolvers (list, get) and mutation resolvers (create, update, delete) for each declared collection. Empty declarations produce a valid schema with _empty placeholder fields. Add 10 new tests: mutation workflow detection, leading-whitespace query parsing, nested variable flattening, empty variables, dynamic schema validity with single/multiple/zero collections, Created status response, and multi-error response translation. All 23 transport-graphql tests pass.

- Architecture Migration WP 9.3 — REST Handler: wire router to dispatch REST routes to real handler functions (handle_with_body for POST/PUT, handle_without_body for GET/DELETE) instead of placeholder closures. Each route injects a HandlerRouteConfig extension carrying the target workflow name, with Identity provided by auth middleware at the application level. Add 5 new handler tests covering all WorkflowStatus variants (Denied→403, Invalid→400), query parameter propagation, JSON body preservation, and request metadata timestamps. Update router tests and integration tests to provide Identity extension and verify the { "data": ... } response envelope shape. All 60 transport-rest tests pass.

- Architecture Migration WP 9.2 — Router and Route Merging: restructure router module into router/merge.rs, router/build.rs, and router/tests.rs. Add route merging with MergedRoute and RouteSource types that combine config routes with plugin manifest routes, detect conflicts (same method+path), validate namespace rules (REST under /api/, GraphQL under /graphql), and collect all errors before returning. Add build_merged_router() that constructs an immutable Axum Router with handler-type awareness (REST vs GraphQL dispatch). Add plugin_id field to PluginRoute for better error attribution in conflict messages. 20 new router tests covering merge, collision detection, namespace validation, path parameter extraction, handler-type dispatch, and end-to-end merge-then-build flow. All 54 transport-rest tests pass.

- Architecture Migration WP 9.1 — Transport Configuration: fix validate_listener() to collect all validation errors (port, TLS, duplicate routes, namespace violations) before returning, instead of short-circuiting on the first error. Add write_default_config() function that serializes the default ListenerConfig to YAML and writes listeners.yaml to a specified config directory. Add serde_yaml dependency to transport-rest. Add 5 new tests: YAML config parsing, YAML defaults application, multi-error validation collection, and default config file round-trip (write + parse + validate). All 39 transport-rest tests pass.

- Architecture Migration WP 5.4 — Audit Logging: add blob storage events (BlobStored, BlobDeleted) and security events (AuthSuccess, AuthFailure, CredentialEvent, PluginUnloaded, ConnectorEvent) as new BusEvent variants in message_bus.rs. Wire audit subscriber in apps/core/src/audit.rs to persist all new event types to the audit_log table via AuditLogger. Auth middleware now publishes AuthSuccess/AuthFailure events on token validation. Credential route handlers publish CredentialEvent on store, retrieve, and delete operations. PluginLoader publishes PluginLoaded, PluginError, and PluginUnloaded events on plugin lifecycle transitions. Event bridge forwards blob events to workflow EventBus while filtering security events as internal-only. SSE event stream filters out internal audit events. Add 8 new audit subscriber tests covering blob stored/deleted, auth success/failure, credential access/modify, plugin unloaded, and connector auth events. All 21 storage-sqlite audit tests pass. Also fix pre-existing build issues: add missing quick-xml dependency to dav-utils, add missing life-engine-traits/sha2/hex/mime_guess dependencies to test-utils, fix transport-webhook Transport::start trait signature mismatch, fix rand 0.9 API drift in rekey.rs, remove unused life-engine-test-utils dev-dependency from core.

- Architecture Migration WP 5.3 — Per-Record Credential Encryption: add packages/crypto/src/credential.rs module providing HKDF-SHA256 based per-record key derivation (derive_credential_key), encrypt_claims, and decrypt_claims functions for transparent credential field encryption. Uses a distinct HKDF info context ("life-engine-credential-encryption-v1") so derived credential keys are independent from database encryption keys. Wire encryption hooks into StorageContext: doc_create and doc_update for the "credentials" collection encrypt the claims field (AES-256-GCM with base64 encoding, sets encrypted:true flag) before delegation to StorageRouter; doc_get and doc_query decrypt on read (strips encrypted flag). Add StorageContext::with_credential_encryption() constructor accepting a Zeroizing<[u8; 32]> master key; existing StorageContext::new() remains backward-compatible with encryption disabled. Add hkdf, base64, serde_json dependencies to packages/crypto and life-engine-crypto, zeroize dependencies to packages/traits. 8 new credential encryption tests (key determinism, per-id isolation, round-trip, wrong-key/id rejection, unique ciphertexts, invalid base64 rejection, empty claims). All 204 tests pass across crypto and traits crates.

- Architecture Migration WP 5.2 — Database Encryption: add packages/storage-sqlite/src/encryption.rs module providing passphrase-based SQLCipher key derivation with Argon2id via packages/crypto. Manages salt persistence in a .salt file alongside the database (16-byte random salt generated on first run, loaded on subsequent runs). Provides derive_db_key() and derive_db_key_with_params() for standard and custom Argon2 parameters, passphrase_from_env() for LIFE_ENGINE_PASSPHRASE environment variable input, and derive_rekey_pair() for passphrase-based key rotation. Add SqliteStorage::from_passphrase() constructor that derives the key and zeroizes the passphrase immediately after derivation, and rekey_with_passphrase() for passphrase-based key rotation delegating to the existing PRAGMA rekey + credential re-encryption flow. 7 new encryption module tests (salt creation, stability, invalid salt rejection, key derivation, determinism, passphrase differentiation, rekey pair generation). All 152 unit + 31 integration tests pass.

- Architecture Migration WP 5.1 — Shared Crypto Crate: add configurable Argon2Params struct (memory_kib, iterations, parallelism with serde support) to packages/crypto/src/types.rs. Add derive_key_with_params() to packages/crypto/src/kdf.rs accepting Argon2Params, refactor existing derive_key() as convenience wrapper using defaults (64 MB, 3 iterations, 4 parallelism). Add hmac_sha256() returning fixed-size [u8; 32] to packages/crypto/src/hmac.rs alongside existing hmac_sign()/hmac_verify(). Re-export Argon2Params, derive_key_with_params, and hmac_sha256 at crate root. Add serde dependency to Cargo.toml. Add 8 new tests (custom params validation, invalid params error, default params assertion, hmac_sha256 consistency, custom-params encrypt-decrypt round-trip). All 28 crypto crate tests pass.

- Architecture Migration WP 4.4 — Storage Context: add doc_watch method to StorageContext implementing the watch-to-event-bus bridge. Subscribes to the adapter's ChangeEvent stream via StorageRouter::doc_watch and spawns a bridge task that re-emits each change as an AuditEvent (system.storage.created/updated/deleted with source:"watch"). Enforces DocRead capability check and collection scoping (plugin-prefix for scoped collections, shared collection declaration check) before subscribing. StorageContext already had full permission enforcement, collection scoping, schema validation on writes, extension namespace isolation, system field injection, and audit event emission from prior implementation. 20 unit tests pass covering capabilities, scoping, schema validation, extension isolation, audit events, system field injection, and watch bridge permission/scoping.

- Architecture Migration WP 4.2 — Filesystem Blob Adapter: wire FsBlobAdapter module into storage-sqlite crate (pub mod blob_fs in lib.rs). Switch MIME type detection from extension-only mime_guess to magic-byte detection via infer crate with mime_guess extension fallback. Add sha2, infer, and mime_guess dependencies to storage-sqlite Cargo.toml. Add infer workspace dependency. Update tests to verify magic-byte detection (PNG header) and extension fallback. 17 blob_fs tests pass.

- Executive Summary WP 3.4 — Add Resource Limits and Connector Resilience: add with_jitter() builder and failure_count() accessor to RetryState in plugin SDK, integrate RetryState into contacts connector. Add 10 MiB blob size limit enforcement in plugin system host_blob_store(). Add WebSocket subscription connection limit (100 concurrent) via atomic counter and RAII guard in GraphQL SubscriptionRoot. Add spawn_cleanup_task() background task to PipelineExecutor for automatic expired job cleanup every 5 minutes. Add decompression bomb protection (256 MiB cap) to backup restore via GzDecoder::take(). Add paginated audit_entries query to GraphQL QueryRoot with AuditLogger::query_entries() and SqliteStorage::with_conn() helper. Add retention_days field to RetentionPolicy with age-based cleanup in enforce_retention(). Fix pre-existing pg_storage password type mismatch and email connector failure_count() call.

- Executive Summary WP 3.3 — Unify Plugin Types and Fix Build Infrastructure: Capability enums already unified (SDK re-exports from traits crate since plugin-sdk WP 1.1). Add CorePlugin vs Plugin documentation to packages/plugin-sdk-rs/src/lib.rs module docs explaining the two plugin models, their use cases (async lifecycle/routes vs synchronous workflow actions), and migration path. Delete stale apps/core/Dockerfile that only copied 4 of 28 workspace members. Fix deploy/docker-compose.yml config mount from config.toml to config.yaml to match the primary YAML-based config parser. Align backup plugin cron dependency from pinned 0.13 to workspace 0.15.

- Executive Summary WP 3.2 — Fix Search, Persistence, and State Management: add user_id and household_id fields to Tantivy search schema with STRING | STORED indexing, extend search() method with optional user_id_filter and household_id_filter parameters using BooleanQuery MUST clauses to prevent cross-tenant search result leakage. Implement batched Tantivy commits with configurable threshold (default 50 documents) via atomic pending_count counter, eliminating per-document write amplification — add flush() method for explicit commit on shutdown. Add SQLite persistence tables for federation_peers (with status, endpoint, cert paths, sync timestamps), federation_sync_cursors (peer_id + collection composite key with CASCADE delete), households, household_members (with role, composite PK, user index), and household_invites (with expiry and accepted flag). Switch production search index from Index::create_in_ram() to disk-backed MmapDirectory via new open_in_dir() constructor at data_dir/search_index, with automatic in-memory fallback on failure. Update all search() callers across routes/search.rs, routes/data.rs, search_processor.rs, and 30+ test call sites. Add flush() call to search processor shutdown drain path.

- Executive Summary WP 3.1 — Implement Webhook Delivery and DAV Protocol Handlers: webhook HTTP POST delivery with HMAC-SHA256 signing, exponential backoff, and configurable timeouts were already implemented by prior WPs (dav-and-webhooks WP 1.5). CalDAV and CardDAV transport crates already had PROPFIND (depth 0/1), REPORT, GET, PUT, DELETE, and MKCALENDAR/MKCOL handlers. Added OPTIONS handlers returning DAV compliance headers (calendar-access for CalDAV, addressbook for CardDAV) and well-known redirect routes (/.well-known/caldav → /caldav/, /.well-known/carddav → /carddav/) per RFC 6764. Fixed Transport::start signature to match updated trait definition. All 28 existing DAV transport tests pass.

- Executive Summary WP 2.4 — Consolidate Cryptography and Fix DAV Serialization: replace duplicate AES-256-GCM encrypt/decrypt implementations in apps/core/src/crypto.rs and plugins/engine/backup/src/crypto.rs with thin wrappers delegating to the canonical life_engine_crypto crate. Add hmac_sha256_verify function to core crypto for constant-time HMAC verification. Add zeroize dependency to packages/crypto and wrap Argon2id-derived keys in Zeroizing<[u8; 32]> to auto-clear key material on drop. Add life-engine-crypto and rand dependencies to backup plugin, remove direct aes-gcm dependency. CalDAV and CardDAV serializer fold_line functions already use char_indices() from prior WPs. All 22 life-engine-crypto tests pass.

- Architecture Migration WP 4.1 — SQLite Document Adapter: implement SqliteDocumentAdapter in packages/storage-sqlite/src/document.rs implementing the full DocumentStorageAdapter trait. Uses Arc<Mutex<Connection>> with tokio::task::spawn_blocking for async wrapping of synchronous rusqlite calls. Stores documents as JSON in the existing plugin_data table. Includes recursive FilterNode-to-SQL query translation via WhereBuilder using json_extract() with parameterised bind values, cursor-based pagination (base64-encoded offsets), JSON merge patch for partial_update, atomic batch operations (create/update/delete) via SQLite transactions, schema migration with advisory index creation on json_extract paths, health checks (connection, WAL mode, write capability), and watch stub returning empty channel. 31 integration tests covering CRUD, query filters (And/Or/Contains/In/StartsWith), sorting, pagination, field projection, batch atomicity (rollback on duplicate), migration idempotency, health reporting, and collection isolation.

- Executive Summary WP 2.2 — Fix Plugin System Execution and Host Functions: wire BlobBackend through load_plugins and load_single_plugin into InjectionDeps (previously hardcoded to None), replacing TODO comment. Replace single global Mutex<HashMap<String, PluginHandle>> in PluginSystemExecutor with per-plugin HashMap<String, Mutex<PluginHandle>> to allow parallel execution of different plugins. Fix pre-existing EventsHostContext declared_subscribe_events field mismatch and WorkflowEventEmitter::emit signature drift in test mocks. Propfind/Report HttpMethod variants already present from prior WP.

- Backup & Webhooks WP 1.9 — Minor Cleanup and Hardening: use HashSet for incremental backup collection deduplication in engine.rs, replace sync path.exists() with tokio::fs::try_exists() in local backend, delete orphan types.rs (WebhookSenderStatus not declared as module), refactor process_webhook to parse JSON from raw_body internally eliminating signature/payload mismatch risk. URL validation and payload hash storage were already implemented by prior agents.

- Executive Summary WP 2.1 — Fix REST Transport Auth and Identity: replace middleware-local Identity struct in packages/transport-rest/src/middleware/auth.rs with life_engine_types::identity::Identity, converting AuthIdentity fields (user_id→subject, provider→issuer, scopes→claims). Fix public route bypass to use MatchedPath instead of raw URI path so parameterized routes are correctly recognized. Add DefaultBodyLimit (2 MB) layer to apps/core/src/main.rs router to prevent unbounded request body consumption. Update middleware tests accordingly.

- Executive Summary WP 1.5 — Fix API Key Material Exposure and Credential Re-encryption: add ApiKeyMetadata type (excludes key_hash and salt) to packages/auth/src/types.rs with From<ApiKeyRecord> conversion. Update list_keys to return Vec<ApiKeyMetadata> instead of Vec<ApiKeyRecord>, extract internal list_key_records for validation use. Implement re_encrypt_credentials in packages/storage-sqlite/src/credentials.rs — reads all credential rows, decrypts with old key, re-encrypts with new key, all within a single SQLite transaction for atomicity.

- Architecture Migration WP 1.3/1.4 — CDM JSON Schemas and Workflow Engine Contract Types: add TriggerContext enum (Endpoint, Event, Schedule variants) to new packages/types/src/trigger.rs with re-export at crate root. WorkflowRequest, WorkflowResponse, WorkflowStatus, Identity, and helpers were already implemented. Add 5 TriggerContext tests. WP 1.3 verified complete — 6 schema files at .odm/doc/schemas/ with 22 passing validation tests. Phase 1 (Foundation Types) now fully complete.

- Architecture Migration WP 1.2 — CDM Rust Structs: add get_ext and set_ext helper functions to packages/types/src/extensions.rs for reading and writing plugin-namespaced extension fields with merge-not-replace semantics. Re-export helpers at crate root. All 6 CDM structs, nested types, and 350 tests were already in place from prior work packages. 8 new extension helper tests added.

- Architecture Migration WP 1.1 — PipelineMessage Core Types: add PipelineEnvelope, PipelineMetadata, StepTrace, StepOutcome, and IdentitySummary types to packages/types/src/pipeline.rs. These implement the new pipeline architecture spec alongside existing types for incremental migration. Add 8 tests covering full-field round-trip, empty/nested/large (1MB+) payloads, trace accumulation, identity projection, and outcome serialisation. Fix pre-existing ContactName test referencing removed display field.

- DevOps WP 3.2 — Config Code Cleanup: change PostgreSQL password field from String (empty default) to Option<String> (None default) in PostgresSettings, update pg_storage and storage_migration usages, add passphrase minimum-length guidance to .env.example.

## 2026-03-28

- QA phase-2 remediation — plugin-sdk WP 1.5: verify all documentation and cleanup tasks already complete — crate-level CorePlugin vs Plugin guide, register_plugin! statelessness docs, PluginContext/ActionContext relationship docs, CredentialStore/CredentialAccess layering docs, Phase 1 stale comment removed, .cargo/config.toml exists. Plugin SDK plan now fully complete (5/5 WPs).

- QA phase-2 remediation — plugin-sdk WPs 1.1-1.4: verify capability enum unification already complete (traits::Capability re-exported as SDK Capability with Display/FromStr, StorageContext uses unified enum). Add SDK capability round-trip test, convenience wrapper tests for WASM host_call bridge, From<serde_json::Error>/From<std::io::Error>/from_anyhow tests, is_retryable classification tests, delete-with-write-only capability enforcement test, where_not_eq filter test, and default limit test. Fix pre-existing CalendarEvent missing sequence field in mock_message.rs.

- QA phase-2 remediation — dav-and-webhooks WP 1.5: implement webhook sender HTTP delivery via reqwest with HMAC-SHA256 payload signing, configurable timeouts (connect/request/total), per-URL token bucket rate limiting, exponential backoff retry using plugin SDK RetryState, replace Vec::drain with VecDeque for O(1) delivery log eviction, fix pre-existing private field access bug in tests, and add 12 new tests for signing, rate limiting, delivery, and config.

- QA phase-3 remediation — connector-plugins WP 1.4: add 429 rate limit retry with Retry-After backoff to Google Contacts API (replace check_rate_limit with handle_rate_limit, wrap all 5 API call sites in retry loops). Add rate limit retry to Google Calendar API list_calendars (list_events and get_event already handled). IMAP connection-rate awareness already implemented (MIN_CONNECT_INTERVAL + enforce_connect_cooldown).

- QA phase-3 remediation — backup-and-webhooks WP 1.9: add URL validation (url crate) on WebhookSubscription, replace full payload storage in DeliveryRecord with SHA-256 hash (payload_hash field), update all callers to pass payload by reference. HashSet dedup in incremental backup and async exists in local backend already done. Unused WebhookSenderStatus type and process_webhook refactor already completed by prior agents.

- QA phase-3 remediation — connector-plugins WP 1.7: cache SMTP transport in SmtpClient struct for connection pooling across batch sends. Reduce email subject log level from info to debug to prevent sensitive information leakage.
- QA phase-3 remediation — connector-plugins WP 1.6: add 30-second connection timeouts to IMAP connect/connect_plain (via tokio::time::timeout), SMTP transport (.timeout()), and Google API clients in contacts and calendar connectors (reqwest::Client::builder().timeout()).
- QA phase-3 remediation — connector-plugins WP 1.10: remove unused duplicate types.rs from email and filesystem connectors (SyncState, FileChange/FileChangeType). Remove unused FileChange::Moved variant from local.rs.
- QA phase-3 remediation — connector-plugins WP 1.5: remove unnecessary head_object check before delete_object (TOCTOU race fix). Pagination already implemented. SDK client caching deferred (conflicts with per-call credential pattern).
- QA phase-3 remediation — connector-plugins WP 1.3: verified all four connectors already have RetryState from plugin SDK. No code changes needed.
- QA phase-3 remediation — connector-plugins WP 1.1: replace direct secret_access_key/access_key_id fields in S3Config with credential_key referencing the credential store. Add S3Credentials struct for runtime credential passing. Update CloudStorageConnector trait and S3Client impl to accept credentials parameter on all operations. Update integration tests to use credential store pattern.
- QA phase-3 remediation — connector-plugins WP 1.9: add symlink cycle detection (via canonical path tracking) and max depth limit (64) to filesystem connector scan_directory. Remove unused notify crate dependency. Remove unnecessary watch_paths clone in scan(). Fix let-chains in should_include for stable Rust compatibility.

- QA phase-3 remediation — connector-plugins WP 1.2: refactor let-chains in connector-calendar handle_event() and google.rs to use nested if-let blocks for Rust stable compatibility. Local urlencoding module already provides explicit implementation (no transitive dependency risk).

- QA phase-2 remediation — dav-and-webhooks WP 1.4: implement webhook transport with Axum router, HMAC-SHA256 signature verification (constant-time comparison), timestamp-based replay protection (5-minute window), idempotency key deduplication, content-type validation, webhook-specific error variants, and 12 integration tests.

- QA phase-3 remediation — core-application WP 1.8 (partial): add tracing::warn when PEM file contains multiple private keys in tls.rs. Remove unnecessary Result wrapper from IdentityStore::new() and update all callers. HKDF salt already present in derive_key. Deferred: audit sleep replacement (requires production code changes), redundant Arc removal (large refactor), health endpoint caching, multi-household prevention (changes return type).

- QA phase-3 remediation — backup-and-webhooks WP 1.1: verified already complete — per-backup random salt, salt-accepting derive_key, salt prepended to payload, restore extracts salt, tests for salt uniqueness all present.

- QA phase-2 remediation — dav-and-webhooks WP 1.2: implement CalDAV transport with Axum router, PROPFIND handler for calendar discovery (depth 0/1), REPORT handler for calendar-query and calendar-multiget, GET/PUT/DELETE handlers with ETag support, MKCALENDAR handler, CalDAV-specific error variants, CalDAV types, and 14 integration tests.

- QA phase-2 remediation — dav-and-webhooks WP 1.3: implement CardDAV transport with Axum router, PROPFIND handler for addressbook discovery (depth 0/1), REPORT handler for addressbook-query and addressbook-multiget, GET/PUT/DELETE handlers with ETag support, MKCOL handler, CardDAV-specific error variants, CardDAV types, and 14 integration tests.

- QA phase-3 remediation — api-plugins WP 1.3: add RFC 6764 well-known endpoint support with 301 redirect builders and PROPFIND route registration on /.well-known/{caldav,carddav}. Add OPTIONS route handlers advertising DAV compliance headers (calendar-access for CalDAV, addressbook for CardDAV) per RFC 4791/6352. Add tests for redirect responses, DAV headers, Allow headers, and route registration for both plugins.

- QA phase-3 remediation — core-application WP 1.5: implement PgSslMode::Prefer with actual TLS fallback behavior in pg_storage.rs. Separate Prefer from Require so it tries TLS first and falls back to plaintext on failure, matching expected PostgreSQL ssl_mode=prefer semantics. Verify rekey::derive_key() zeroed-salt fallback already guarded by #[cfg(test)].

- Architecture implementation across all packages: expand plugin manifest parsing with collections, events, trust levels, and resource limits. Extend audit logging with storage write events and security events. Add new trait modules (index_hints, schema, schema_versioning, blob, storage_context, storage_router). Restructure transport-rest with config, middleware, and router modules. Expand workflow engine with concurrency limits, job registry, condition operators, and trigger system. Add blob storage, health checks, and migration executor to storage-sqlite. Add identity and workflow types. Add schema documentation and QA reports. Update Cargo dependencies across workspace.

## 2026-03-27

- Remove sidecar/bundled architecture — Core is now standalone-only. Delete `apps/app/` Tauri desktop client directory. Remove bundled-mode code from Core (`is_bundled_mode()`, `apply_bundled_defaults()`, `bundled_data_dir()` in config.rs, `"bundled"` branch in `detect_deployment_mode()` in main.rs). Update deployment-modes spec from 4 to 3 modes (standalone, Docker, home server). Supersede ADR-002 (Tauri v2 for App), create ADR-015 (Remove Sidecar Architecture). Update ADR-001 and ADR-007 to remove sidecar references. Update design docs (Core Overview, Client Interface, Principles, Life Engine), planning docs (Phase 10, risk register, overview, Phase 1), and monorepo spec. Remove Tauri CI job from ci.yml.disabled, Tauri test from build_verification_test.rs, Tauri entries from .gitignore, and `apps/app/src-tauri` exclude from Cargo.toml. Update README.md, pnpm-workspace.yaml.

## 2026-03-23

- Update planning docs and add ARM64/QA support files: align planning overview, risk register, success criteria, and test plan with thin-Core + WASM plugin architecture. Replace Obsidian wiki-links with relative markdown links. Mark WP 10.24 complete in progress.json. Add .cargo/config.toml for ARM64 cross-compilation (aarch64-unknown-linux-gnu and aarch64-unknown-linux-musl linker config). Add deploy/arm64-build.md with Docker multi-arch and native cross-compilation instructions. Add .odm/qa/ISSUES_REPORT.md with initial QA findings (1 test failure, 3 wasmtime vulns, 104 clippy warnings, 43 crate duplicates). Update Cargo.lock with life-engine-storage-sqlite and rusqlite dependencies for workflow-engine.

- WP 10.24 Private Collection Support Registration: add SchemaRegistry struct in packages/workflow-engine/src/schema_registry.rs for plugin private collection schema management. Implement thread-safe registry using RwLock<HashMap<(String, String), serde_json::Value>> keyed by (plugin_id, collection_name) tuple. Three public methods: register(plugin_id, collection, schema) stores raw JSON Schema values with canonical CDM name rejection (events, tasks, contacts, notes, emails, files, credentials), get_schema(plugin_id, collection) returns cloned Value with namespace isolation (plugins can only access their own schemas), is_registered(plugin_id, collection) checks registration state. SchemaRegistryError enum with ReservedCollection and LockPoisoned variants using thiserror. Module registered in workflow-engine lib.rs with pub re-exports of SchemaRegistry and SchemaRegistryError. 8 tests: registration succeeds, schema lookup works with content verification, unregistered collection returns None, cross-plugin access denied, all 7 canonical names rejected, is_registered reflects state correctly, multiple plugins with same-named collections isolated, schema overwrite replaces value. All 145 workflow-engine tests pass (137 existing + 8 new), build clean.

- WP 10.23 First-Party Plugin WASM Conversion: implement WASM-compatible Plugin trait and register_plugin! macro entry point for all 7 first-party engine plugins (connector-email, connector-calendar, connector-contacts, connector-filesystem, webhook-sender, search-indexer, backup). Each plugin now has dual-trait support: CorePlugin for native lifecycle management and Plugin for WASM execution. Added EngineError implementations to all plugin error types with structured error codes and severity classification. Plugin::execute() routes declared actions from manifest.toml, returning UnknownAction errors for unrecognized actions. Added register_plugin! macro calls (gated behind cfg(target_arch = "wasm32")) for Extism WASM entry-point generation. Disambiguated all existing test method calls for dual-trait coexistence. Added 28 new WASM Plugin trait tests across all 7 plugins (4 per plugin: id matches core, actions list, execute known action, execute unknown action). All 506 plugin tests pass, build clean.

- WP 10.22 Migration Integration Tests: add 8 end-to-end integration tests in packages/workflow-engine/tests/migration_test.rs exercising the full migration pipeline. Tests cover: (1) simple migration — insert records at v1, run identity WASM transform, verify records bumped to v2, backup created, migration_log entry written. (2) quarantine — trapping WASM transform quarantines all matching records with correct metadata (plugin_id, collection, from/to versions). (3) chain migration — three sequential entries (v1→v2→v3→v4) applied in a single invocation, verify records end at v4 with data preserved, 3 migration_log entries written. (4) backup and restore — run migration, verify backup exists, call restore_backup(), verify records revert to original version and data. (5) idempotency — run same migration twice, second run finds no matching records (already at target version), no duplicate transforms. (6) collection scoping — migration targeting "events" leaves "tasks" records untouched. (7) plugin isolation — migration for plugin-a does not affect plugin-b records in same collection. (8) no matching records — migration completes gracefully with zero counts when no records match the from version. All 137 workflow-engine tests pass (129 unit + 8 integration).

- WP 10.21 Version Column Update: add stamp_version() function in packages/storage-sqlite/src/migration/version.rs for post-transform version stamping. Implements pub fn stamp_version(conn: &Connection, record_id: &str, new_version: &str) -> Result<(), StorageError> that parses the major version from a semver string and runs UPDATE plugin_data SET version = ? WHERE id = ?. Returns StorageError::NotFound if the record doesn't exist, StorageError::InvalidConfig if version parsing fails. Designed to be called within the same SQLite transaction as the data update to ensure atomicity — after stamping, the record's version no longer matches the migration entry's from range, preventing re-migration on subsequent startups. 5 tests: version updated after successful transform, version update atomic with data update (rollback reverts both), re-running migration skips already-migrated records, errors for missing record, parses major from semver. All 9 version module tests pass (4 existing + 5 new), full build clean.

- WP 10.20 Canonical Migration Path: add schema version tracking and canonical migration startup trigger. Add schema_versions table DDL to packages/storage-sqlite/src/schema.rs with columns: plugin_id (TEXT NOT NULL), collection (TEXT NOT NULL), version (INTEGER NOT NULL DEFAULT 1), updated_at (TEXT NOT NULL), PRIMARY KEY (plugin_id, collection). Add SCHEMA_VERSIONS_DDL to ALL_DDL array. Create packages/storage-sqlite/src/migration/version.rs with get_schema_version() and set_schema_version() CRUD operations using INSERT OR REPLACE for upsert semantics. Create packages/types/src/migrations.rs declaring CANONICAL_PLUGIN_ID ("core"), CanonicalCollection struct (name, version, migration_dir), and CANONICAL_COLLECTIONS array listing all 7 canonical collections (events, tasks, contacts, notes, emails, files, credentials) at version 1. Create packages/types/migrations/ directory structure with one subdirectory per canonical collection for future WASM transform binaries. Add canonical migration startup trigger in apps/core/src/main.rs as step 4b (between storage init and auth init): checks each canonical collection's stored version against declared version, stamps version on first run, collects pending migrations where stored < declared, runs WASM transforms via the standard migration engine (same sandbox, quarantine, backup, logging), stamps new version on success. Add schema_versions table to apps/core/src/sqlite_storage.rs create_tables(). 5 version tests: get returns None for unknown, set then get, set overwrites, scoped by plugin and collection. 1 schema test: schema_versions DDL creates table. 3 migrations tests: unique names, positive versions, canonical plugin id. All tests pass, build clean.

- WP 10.19 Migration Execution Engine: add core migration execution loop in packages/workflow-engine/src/migration/engine.rs. Implement run_migrations(wasm_path, entries, plugin_id, db_path, data_dir) that orchestrates the full migration pipeline: (1) create pre-migration backup via create_backup(), (2) sort entries by target version ascending, (3) for each entry: query plugin_data records matching plugin_id, collection, and version (major number from entry.from), begin SQLite transaction, transform each record via WASM sandbox run_transform(), update successful records' data and version columns, quarantine failed records with full error metadata, commit transaction, (4) log each entry result to migration_log table, (5) return MigrationResult with migrated/quarantined counts, duration_ms, backup_path, and entries_applied list. Add MigrationResult struct. Add parse_from_version() helper to extract major version number from semver-like from strings (1.0.0, 2.x, 3). Add life-engine-storage-sqlite and rusqlite as dependencies to workflow-engine Cargo.toml. Register engine module in migration/mod.rs with re-exports. 6 tests: parse_from_version extracts major correctly, rejects invalid input, no matching records returns zero counts with backup, transforms matching records and updates version, quarantines failed transforms, logs to migration_log table. All 129 workflow-engine tests pass, build clean with no warnings.

- WP 10.18 Pre-Migration Backup: add pre-migration SQLite backup and restore mechanism in packages/storage-sqlite/src/migration/backup.rs. Implement create_backup(db_path, data_dir) that creates a backups/ subdirectory, generates a timestamped backup filename (pre-migration-YYYY-MM-DD-HHmmss.db), uses rusqlite's online backup API (Backup::new + step(-1)) for atomic copy without blocking readers, verifies the backup with PRAGMA integrity_check, and returns the backup path. Implement restore_backup(backup_path, db_path) for manual admin recovery that verifies backup integrity before copying. Add StorageError::Io variant (STORAGE_012, Fatal) for filesystem errors with From<std::io::Error> derive. Enable rusqlite "backup" feature in workspace Cargo.toml. Register backup module in migration/mod.rs. 4 tests: create produces valid copy with same data, creates backups directory automatically, restore replaces modified database with original, restore fails for missing file. All 102 storage-sqlite tests pass, full workspace builds clean.

- WP 10.17 Migration Log Table and Operations: add migration_log table DDL and CRUD operations in packages/storage-sqlite/src/migration/log.rs. Create MIGRATION_LOG_DDL in schema.rs with columns: id (TEXT PRIMARY KEY, UUID), plugin_id (TEXT NOT NULL), collection (TEXT NOT NULL), from_version (TEXT NOT NULL), to_version (TEXT NOT NULL), records_migrated (INTEGER NOT NULL), records_quarantined (INTEGER NOT NULL), duration_ms (INTEGER NOT NULL), backup_path (TEXT, nullable), error (TEXT, nullable), timestamp (TEXT NOT NULL, ISO 8601). Add composite index idx_migration_log_plugin_collection on (plugin_id, collection). Implement three operations: log_migration() inserts a full MigrationLogEntry for successful or partially successful runs, log_failure() records migration failures that prevented execution entirely (zero counts, error message, auto-generated UUID and timestamp), get_migration_history() retrieves all log entries for a plugin/collection pair ordered by timestamp ascending for admin review. Add MIGRATION_LOG_DDL to ALL_DDL array. Register log module in migration/mod.rs. 8 log tests: insert entry, store all fields correctly, log failure with zero counts, filter by plugin and collection, empty result for no matches, null backup path, error field support, multiple migrations ordered by timestamp. 1 schema test: migration_log DDL creates table and index. All tests pass, cargo check clean.

- WP 10.16 Quarantine Table and Operations: add migration quarantine table schema and CRUD operations in packages/storage-sqlite/src/migration/quarantine.rs. Create quarantine table DDL in schema.rs with columns: id (TEXT PRIMARY KEY, UUID), record_data (TEXT NOT NULL, original JSON record), plugin_id (TEXT NOT NULL), collection (TEXT NOT NULL), from_version (TEXT NOT NULL), to_version (TEXT NOT NULL), error_message (TEXT NOT NULL), timestamp (TEXT NOT NULL, ISO 8601). Add composite index idx_quarantine_plugin_collection on (plugin_id, collection). Implement four CRUD operations: quarantine_record() inserts a failed record and returns UUID, list_quarantined() retrieves all quarantined records for a plugin/collection pair ordered by timestamp, retry_quarantined() fetches a quarantined record's JSON data for re-migration, delete_quarantined() removes a record after successful retry or admin dismissal. Create migration/ module directory under storage-sqlite. Add QUARANTINE_DDL to ALL_DDL array so the table is created during storage initialization. 9 tests: insert and return id, list matching records filtered by plugin and collection, list returns empty for no matches, retry returns parsed JSON data, retry errors for missing id, delete removes record, delete errors for missing id, unique id generation, all fields stored correctly. 1 schema test: quarantine DDL creates table and index. All 89 storage-sqlite tests pass, clippy clean.

- WP 10.14 WASM Export Validation: add validate_wasm_exports() and validate_wasm_exports_from_bytes() functions in packages/workflow-engine/src/migration/validate.rs. Uses wasmparser to parse the WASM binary's export section without instantiating the module. For each MigrationEntry, checks that the declared transform function name exists as a function export (not memory, global, or table exports). Returns MigrationError::ManifestValidation listing all missing export names if any are absent. Validates at plugin load time — a plugin with missing migration transform exports is rejected before it can run. Empty entries list always passes. Add wasmparser workspace dependency to workflow-engine Cargo.toml and wat dev-dependency for tests. 8 tests: all exports present, missing single export, all exports missing, empty entries always passes, invalid WASM binary, non-function exports ignored, validate from file, validate from nonexistent file. All 117 workflow-engine tests pass, clean build.

- WP 10.13 Migration Entry Overlap Detection: add overlap detection for migration from ranges in packages/workflow-engine/src/migration/manifest.rs. Implement VersionRange enum (MajorWildcard, MinorWildcard, Exact) with overlaps() method that determines whether any concrete version could match both ranges. Add validate_no_overlapping_ranges() function that groups entries by collection and checks all pairs within each collection for overlapping from ranges — rejects plugin updates with MigrationError including both conflicting ranges and an example version that matches both. Overlap rules: MajorWildcard overlaps MajorWildcard if same major, MajorWildcard overlaps MinorWildcard/Exact if same major, MinorWildcard overlaps MinorWildcard if same major+minor, MinorWildcard overlaps Exact if same major+minor, Exact overlaps Exact only if all components match. Different collections are independent — same from range across different collections is allowed. 7 new tests: non-overlapping ranges pass, overlapping major+minor wildcard fails, same from range different collections allowed, exact version ranges do not overlap, identical exact versions overlap, non-overlapping minor wildcards pass, overlap error includes example version. All 33 manifest tests pass, clean build.

- WP 10.12 Migration Manifest Validation: add migration entry parsing and validation module in packages/workflow-engine/src/migration/. Create MigrationEntry struct with from (semver range), to (exact semver), transform (WASM export name), description, and collection fields. Implement parse_migration_entries() and parse_migration_entries_from_str() that parse [[migrations]] TOML array with six validation rules: (1) from must be a valid simplified semver range (major.x, major.minor.x, or exact major.minor.patch), (2) to must be an exact semver version, (3) from minimum version must be strictly less than to, (4) transform name must be a valid Rust identifier ([a-zA-Z_][a-zA-Z0-9_]*), (5) collection name must contain only alphanumeric, hyphens, underscores, and colons, (6) migration chain must be contiguous within each collection — the to version of one entry must match the from range of the next. Add MigrationError enum implementing EngineError trait with ManifestParse (MIGRATION_001), ManifestValidation (MIGRATION_002), TransformFailed (MIGRATION_003), and TransformCrashed (MIGRATION_004) variants. Add toml and semver workspace dependencies to workflow-engine Cargo.toml. 26 tests covering all validation paths pass, clean build with no warnings.

- WP 10.11 Network Security Enforcement: add non-localhost auth provider validation in apps/core/src/config.rs validate() method. When bind address is non-localhost (0.0.0.0, ::, or any non-127.0.0.1 IP), reject local-token auth provider with "Refusing to start" error requiring oidc or webauthn — prevents accidentally exposing an unauthenticated instance to the network. Update TLS enforcement error message to match "Refusing to start" format for consistency. Add security posture log line in apps/core/src/main.rs after logging init that prints TLS, Auth, RateLimit, and BindAddress in a single structured log entry for operator visibility. Replace dead-code non-localhost TLS warning (unreachable since validate() already blocks this case) with the security posture log. All existing tests pass, cargo check clean.

- WP 10.10 Tauri Sidecar Integration: create apps/app/src-tauri/ directory with full Tauri v2 desktop app configuration for bundled deployment mode. tauri.conf.json: Core binary as sidecar in bundle.externalBin, bundled plugins/ and workflows/ in resources, 1200x800 window with CSP allowing localhost connections. src/main.rs: (1) spawn Core as sidecar process via tauri-plugin-shell with LIFE_ENGINE_BUNDLED=true env var, platform-standard data dir, auto-generated passphrase, JSON log format; (2) pipe sidecar stdout/stderr to app tracing; (3) poll Core /api/health endpoint (500ms intervals, 30s timeout) before showing main window; (4) on exit, send kill signal and wait 5s grace period. Passphrase stored in data_dir/.passphrase with 0o600 Unix permissions (platform keychain integration deferred). Add bundled-mode detection to apps/core/src/config.rs: is_bundled_mode() checks LIFE_ENGINE_BUNDLED env var, apply_bundled_defaults() sets platform App data directory via directories::ProjectDirs (macOS ~/Library/Application Support/life-engine/, Linux $XDG_DATA_HOME/life-engine/, Windows %APPDATA%/life-engine/), forces localhost binding, JSON log format, and encryption enabled. Update detect_deployment_mode() in main.rs to check LIFE_ENGINE_BUNDLED alongside existing TAURI_ENV/TAURI vars. Core compiles clean, all existing tests pass.

- WP 10.9 ARM64 Build Verification: add deploy/verify-arm64.sh automated verification script that validates the ARM64 build pipeline. Script runs 11 checks across three categories: (1) Configuration checks — .cargo/config.toml has aarch64-unknown-linux-gnu and aarch64-unknown-linux-musl targets, Dockerfile uses Alpine multi-arch base images, rusqlite uses bundled-sqlcipher for architecture-independent SQLCipher compilation. (2) Native build checks — cargo check --release succeeds, cargo build --release produces executable binary, binary architecture is ARM64, binary executes --help, health check responds at /api/system/health. (3) Docker build checks — docker buildx builds linux/arm64 image, image size under 50 MB, binary executes inside ARM64 container. Script supports --native, --docker, and auto modes. Verified all 11 checks pass on Apple Silicon (arm64) host with binary building, running, and responding to health probes.

- WP 10.8 Behind-Proxy Support: add behind_proxy (bool, default false) field to NetworkSettings in apps/core/src/config.rs. When behind_proxy is true or LIFE_ENGINE_BEHIND_PROXY=true env var is set: (1) skip TLS requirement for non-localhost bind addresses (the reverse proxy handles TLS), (2) trust X-Forwarded-For headers for client IP extraction, (3) trust X-Forwarded-Proto for protocol detection. When behind_proxy is false and bind address is not localhost: enforce TLS configuration — refuse to start without network.tls with cert_path and key_path. Add behind_proxy to startup configuration summary log in apps/core/src/main.rs. Log informational message when behind-proxy mode is active. Update non-localhost TLS warning to skip when behind_proxy is true.

- WP 10.6 Install-Service CLI Subcommand: add `life-engine-core install-service` CLI subcommand in apps/core/src/install_service.rs. On Linux (requires root): creates life-engine system user/group if they don't exist, creates /var/lib/life-engine data directory with correct ownership, copies deploy/systemd/life-engine-core.service to /etc/systemd/system/, runs systemctl daemon-reload and enable, prints next-steps instructions for passphrase configuration and service start. On macOS: creates ~/Library/Application Support/life-engine/ data directory and ~/Library/Logs/life-engine/ log directory, copies deploy/launchd/com.life-engine.core.plist to ~/Library/LaunchAgents/, loads the agent via launchctl, prints configuration instructions. Unsupported platforms get a clear error message. Add CliCommand enum with InstallService variant to CliArgs in config.rs using clap's subcommand derive. Intercept subcommand in main() before server startup. Service file discovery checks relative to executable path and current directory.

- WP 10.3 Standalone Binary Configuration: implement platform-specific config file discovery in apps/core/src/config.rs startup module. Config file resolution follows priority order: (1) --config CLI argument, (2) LIFE_ENGINE_CONFIG env var, (3) platform-specific default via directories crate (Linux: $XDG_CONFIG_HOME/life-engine/config.toml, macOS: ~/Library/Application Support/life-engine/config.toml, Windows: %APPDATA%\life-engine\config.toml). Add default_config_dir() and default_config_file() public functions using the directories crate for runtime path resolution. When no config file exists, create the config directory and write a starter config.toml with commented-out sections explaining each option. Add Windows %APPDATA% expansion in expand_tilde(). Add deployment mode detection in apps/core/src/main.rs via detect_deployment_mode() which checks for Docker (/.dockerenv, container env var), Tauri sidecar (TAURI_ENV, TAURI env var), or defaults to standalone. Log a startup configuration summary after logging init with deployment mode, bind address, TLS status, auth provider, database path, plugins/workflows directories. Warn when running without TLS on a non-localhost address.

- WP 10.2 Docker Compose Configuration: update deploy/docker-compose.yml with bind-mount volumes for data (./data:/data), plugins (./plugins:/plugins), workflows (./workflows:/workflows), and config (./config.toml:/app/config.toml:ro). Add environment variables for all config overrides (LIFE_ENGINE_STORAGE_PATH, LIFE_ENGINE_STORAGE_PASSPHRASE from .env, LIFE_ENGINE_TRANSPORTS_REST_PORT). Set 256M memory resource limit. Update Dockerfile path from apps/core/Dockerfile to deploy/Dockerfile. Create deploy/.env.example documenting all available environment variables. Update deploy/docker-compose.full.yml with matching volume mounts, env vars, resource limits, and Dockerfile path. Both compose files validate cleanly.

- WP 9.11 Structured Logging: enhance init_logging() in apps/core/src/main.rs to configure tracing subscriber with common fields and per-module log level overrides. Add root span with version (from CARGO_PKG_VERSION) and pid (process ID) fields injected into every JSON log entry via a leaked span guard. Build EnvFilter directive string from base log level plus per-module overrides from core.log_modules config — module names are automatically mapped to crate names (e.g. "storage" → "life_engine_storage_storage", already-prefixed names pass through). Add log_modules field (HashMap<String, String>) to CoreSettings in config.rs and modules field to startup LoggingConfig. Add structured_logging_test.rs with 5 tests: log_modules YAML deserialization, empty log_modules default, filter directive building with per-module overrides, no double-prefix for already-qualified crate names, TOML-based startup LoggingConfig modules parsing. All core tests pass, workspace compiles clean.

- WP 8.20 Community Plugin Loading Test: add community plugin loading integration tests in packages/plugin-system/tests/community_plugin.rs validating the full third-party plugin lifecycle from discovery to capability-gated loading. 6 tests: community_plugin_discovered_in_same_plugins_directory (third-party plugin directory scanned via same scan_plugins_directory as first-party, loads successfully with no capabilities declared — validates spec 9.1), community_plugin_rejected_without_explicit_approval (plugin declaring storage:read with no config approval is rejected — validates spec 9.2), approved_community_plugin_gets_same_host_functions_as_first_party (loads both a first-party and community plugin with identical capabilities, asserts injected_function_names are identical — validates spec 9.3), community_and_first_party_plugins_coexist (3 plugins loaded in same directory all discovered and loaded), community_plugin_partial_approval_rejects_entirely (plugin declaring 3 capabilities with only 2 approved is rejected entirely due to unapproved http:outbound), community_plugin_approval_enables_full_loading (fully approved community plugin loads with correct capabilities and injection gating verified — storage:read, events:emit, config:read approved, unapproved functions absent). All 145 plugin-system tests pass (103 unit + 42 integration).

- WP 8.19 Plugin-to-Plugin Communication Tests: add plugin-to-plugin communication integration tests in packages/plugin-system/tests/communication.rs validating the three inter-plugin communication paths. 5 tests: workflow_chaining_passes_output_as_next_input (two echo plugins wired as a two-step WorkflowDef via PipelineExecutor, verifies correlation_id, source, and Contact payload survive the chain), shared_canonical_collection_visible_to_both_plugins (plugin-a writes Contact via host_storage_write, plugin-b reads contacts via host_storage_read through shared RecordingStorage backend, verifies both plugins access the same canonical collection with correct plugin_id scoping), no_host_function_exists_for_direct_plugin_calls (exhaustive check that injected_function_names with all capabilities returns exactly 7 functions — none named call_plugin/invoke_plugin/plugin_call/plugin_invoke), plugin_cannot_execute_another_plugin_through_executor (plugin-a calling plugin-b's action via PluginSystemExecutor returns "unknown action" error, confirming action isolation per plugin manifest), storage_scoping_prevents_plugin_impersonation (spoofed plugin_id in StorageMutation and StorageQuery overwritten to actual caller by host functions). Test fixtures use WAT-compiled echo WASM with write-contact and read-and-note exports. All plugin-system tests pass.

- WP 8.18 Capability Enforcement Integration Tests: add end-to-end capability enforcement integration tests in packages/plugin-system/tests/capability_enforcement.rs validating the full plugin loading pipeline with capability enforcement. 5 tests: first_party_plugin_auto_granted_all_declared_capabilities (first-party plugin loaded via load_plugins with all 6 capabilities declared, all auto-granted without config entries, verified via ApprovedCapabilities.has() and injected_function_names returns all 7 functions), third_party_approved_capabilities_load_successfully (third-party plugin with storage:read declared and approved in config loads successfully, verified only host_storage_read and host_log injected), third_party_plugin_lacks_unapproved_capabilities (third-party plugin with storage:read approved verified to NOT have storage:write, http:outbound, events:emit, events:subscribe, or config:read, injection gating confirmed via injected_function_names), third_party_unapproved_manifest_capability_refuses_to_load_with_cap_001 (third-party plugin declaring storage:read+storage:write with only storage:read approved is rejected entirely, load_plugins returns empty vec, direct check_capability_approval call confirms CAP_001 error code with Fatal severity and error message naming both the unapproved capability and plugin ID), approving_capability_in_config_allows_plugin_to_load (two-phase test: first load with partial approval rejects plugin, second load with full approval succeeds — simulates config modification enabling load on restart). Test fixtures use WAT-compiled echo WASM module and mock StorageBackend/WorkflowEventEmitter implementations. All 134 plugin-system tests pass (103 unit + 5 capability enforcement + 7 injection + 13 runtime capability + 6 crash isolation).

- WP 8.17 Crash Isolation Verification: add crash isolation integration tests in packages/plugin-system/tests/crash_isolation.rs validating Extism WASM sandboxing guarantees. 6 tests: panicking_plugin_returns_error_without_crashing_core (WASM unreachable trap surfaces as ExecutionFailed PLUGIN_007 with Retryable severity, Core process continues), error_is_logged_with_panicking_plugin_id (error message includes action context for tracing correlation), healthy_plugin_continues_after_crash (echo plugin executes normally before and after a sibling plugin crashes), lifecycle_manager_can_unload_crashed_plugin (force_unload transitions crashed plugin to Unloaded, executor rejects subsequent calls with "not running"), wasm_memory_isolation_prevents_cross_plugin_corruption (echo plugin produces identical output before and after crash — separate WASM linear memories), crashed_plugin_can_be_retried (Extism instance survives trap and can be invoked again). Test fixtures use WAT-compiled WASM modules: panicking module exports execute with unreachable instruction, echo module copies input to output. All 129 plugin-system tests pass (103 unit + 7 injection + 13 runtime capability + 6 crash isolation).

- WP 8.16 Runtime Capability Checks: add defence-in-depth runtime capability enforcement as the second layer of the two-layer capability system. Add RuntimeCapabilityViolation variant to PluginError with error code CAP_002 and Severity::Fatal, distinct from the load-time CapabilityViolation (now CAP_001). Update all host functions (storage read/write, config read, events emit/subscribe, HTTP outbound) to return RuntimeCapabilityViolation instead of CapabilityViolation when a plugin invokes a host function it was not granted at runtime. This ensures that even if injection gating is somehow bypassed, each host function independently enforces capability requirements. Update existing unit tests in storage.rs, config.rs, events.rs, and http.rs to match new error variant. Add runtime_capability_test.rs integration test suite with 13 tests: approved capability allows execution and returns data for all host functions, unapproved capability returns Fatal EngineError with CAP_002 code for all host functions, synchronous check verification (no async runtime required), and error message includes both capability name and plugin ID. All 123 plugin-system tests pass (103 unit + 7 injection + 13 runtime capability).

- WP 8.15 Host Function Injection Gating: implement injection gating layer in packages/plugin-system/src/injection.rs that builds Extism Function objects per-plugin based on approved capabilities. InjectionDeps struct bundles shared resources (storage backend, event bus, log rate limiter, plugin config). build_host_functions() maps each approved capability to its corresponding Extism host function: StorageRead→host_storage_read, StorageWrite→host_storage_write, HttpOutbound→host_http_request, EventsEmit→host_events_emit, EventsSubscribe→host_events_subscribe, ConfigRead→host_config_read. host_log is ALWAYS injected regardless of capabilities. Each function wraps the existing host function implementation in an Extism Function with UserData context and life_engine namespace. Async host functions use tokio runtime bridging. injected_function_names() utility returns expected function names for a given capability set. Update loader.rs to wire injection layer: load_single_plugin() now accepts storage, event_bus, and log_rate_limiter references, constructs InjectionDeps with plugin-specific config, and calls build_host_functions() instead of empty Vec::new(). Remove underscore prefixes from load_plugins() storage/event_bus parameters. Add injection_test.rs integration tests: storage_read_only gets read not write, storage_write without read excludes read, no capabilities gets only logging, all capabilities gets all 7 functions, host_log always present across all combinations, all functions have life_engine namespace, injected_function_names matches build_host_functions output. All 110 tests pass (103 unit + 7 integration), zero warnings.

- WP 8.13 Plugin Config Section Parser: add PluginInstanceConfig struct to the startup config module in apps/core/src/config.rs. PluginInstanceConfig has two fields: approved_capabilities (Vec<String>) for third-party plugin capability approval, and config (HashMap<String, toml::Value>) using #[serde(flatten)] to capture all plugin-specific configuration values. Update PluginsConfig.config from HashMap<String, toml::Value> to HashMap<String, PluginInstanceConfig>. Update mirror type in apps/core/tests/config_test.rs. Update existing startup tests to use new typed access (e.g., .config.get("key") instead of .get("key")). Add approved_capabilities to the full config deserialization test. 5 new tests: plugins_path_parsed_correctly, per_plugin_approved_capabilities_extracted, plugin_specific_config_values_accessible (verifies approved_capabilities is NOT in flattened config HashMap), missing_plugins_section_uses_defaults, plugin_with_no_approved_capabilities_gets_empty_vec. All 726 core tests pass, workspace builds clean.

- WP 8.12 Plugin Execution Bridge: implement PluginSystemExecutor in packages/plugin-system/src/execute.rs bridging the workflow engine's PluginExecutor trait to loaded WASM plugins. PluginSystemExecutor holds Mutex-wrapped HashMap of PluginHandle instances keyed by plugin ID and a Mutex-wrapped LifecycleManager. On execute(): (1) checks LifecycleManager state is Running, returning error for non-running or missing plugins, (2) validates requested action exists in plugin manifest, (3) serializes input PipelineMessage to JSON bytes, (4) calls WASM export via PluginInstance::call(), (5) deserializes output bytes back to PipelineMessage. All WASM errors propagate as PluginError::ExecutionFailed with Severity::Retryable. Add async-trait to plugin-system regular dependencies. Export PluginSystemExecutor from lib.rs. 6 tests: valid action returns PipelineMessage (echo WASM round-trip), unknown action error with action name, plugin not running returns state error, plugin not found returns error, WASM execution error propagates with PLUGIN_007 code and Retryable severity, execution errors have Retryable severity. All 98 plugin-system tests pass, workspace builds clean.

- WP 8.11 Lifecycle Manager: implement six-phase plugin lifecycle state machine in packages/plugin-system/src/lifecycle.rs. Define LifecycleState enum with six phases: Discovered, Loaded, Initialized, Running, Stopped, Unloaded. LifecycleManager struct tracks per-plugin state with insertion-ordered iteration for deterministic start/stop ordering. transition() validates forward-only state transitions (Discovered→Loaded→Initialized→Running→Stopped→Unloaded), returning PluginError::LifecycleError for invalid transitions. force_unload() moves any plugin directly to Unloaded for error recovery. start_all() transitions all Discovered plugins through Load→Init→Running, force-unloading on failure. stop_all() transitions all Running plugins through Stop→Unload in reverse registration order. all_states() returns a snapshot of plugin states. Add LifecycleError variant to PluginError with code PLUGIN_008 and Severity::Fatal. Export LifecycleManager and LifecycleState from lib.rs. 10 tests: full lifecycle, start_all, stop_all reverse order, invalid transition rejected, force unload from any state, logged transitions, unknown plugin error, start_all skips non-discovered, stop_all skips non-running, all_states snapshot. All 92 plugin-system tests pass.

- WP 7.17 Execution Logging: add structured execution logging with per-step timing to packages/workflow-engine/src/executor.rs. Define ExecutionLog, StepLog, StepErrorLog structs and ExecutionStatus, StepStatus enums (all Serialize/Deserialize). ExecutionLog captures workflow_id, trigger_type, trigger_value, started_at, completed_at, total_duration_ms, overall status (Completed/Failed/PartiallyFailed), and per-step StepLog entries. StepLog records index, plugin_id, action, status (Completed/Failed/Skipped/Retried), duration_ms, optional StepErrorLog (message, code, severity, truncated input_summary), and optional retry_count. Instrument run_steps() to capture start/end timestamps, derive trigger info from WorkflowDef, collect step logs via execute_steps(), compute overall status (PartiallyFailed when skipped steps exist), and emit the ExecutionLog as a structured tracing event — info level for success/partial, error level for failures. Update execute_steps() to accept mutable Vec<StepLog> and record timing and status for each step across all error strategies (halt, skip, retry with fallback). Add summarize_input() helper for truncated input serialization in error logs. Export all new types from lib.rs public API. All 76 workflow engine tests pass, full project builds clean.

- WP 7.15 Cron Scheduler: implement Scheduler struct in packages/workflow-engine/src/scheduler.rs for time-based workflow triggering. Scheduler::start() accepts TriggerRegistry, PipelineExecutor, and WorkflowEventEmitter, iterates all schedule entries from the registry, and spawns a tokio task for each cron-triggered workflow. Each task loops: calculates next fire time from the cron expression using the cron crate, sleeps until that time via tokio::time::sleep(), then triggers the workflow with a Schedule TriggerContext. Failed workflow executions emit "workflow.schedule.failed" events via the event bus but never stop the scheduler. Scheduler::stop() aborts all task handles and awaits their completion for graceful shutdown. task_count() reports the number of active scheduled tasks. Export Scheduler from lib.rs public API. 4 new tests: scheduler_fires_workflow_on_schedule (every-second cron fires at least once within 1.5s), scheduler_continues_after_workflow_failure (failed executions emit workflow.schedule.failed events, scheduler keeps running), scheduler_stop_cancels_all_tasks (no executions occur after stop), scheduler_with_no_schedules_starts_empty (endpoint-only workflows produce zero scheduled tasks). All 76 workflow engine tests pass.

- WP 7.14 Event Bus: implement EventBus in packages/workflow-engine/src/event_bus.rs using tokio::sync::broadcast channel for fan-out event distribution. EventBus struct holds broadcast sender, Arc<TriggerRegistry>, and Arc<PipelineExecutor>. emit_event() looks up matching workflows via TriggerRegistry::find_event(), spawns independent tokio tasks for each matching workflow with Event TriggerContext, and broadcasts (event_name, payload) tuples to all subscribers. subscribe() returns a broadcast::Receiver for external consumers (logging, metrics). Implements WorkflowEventEmitter trait so PipelineExecutor can use EventBus for async workflow completion/failure events. System events supported: plugin.loaded, plugin.error, workflow.completed, workflow.failed, storage.error. Export EventBus from lib.rs public API. 5 new tests: emit_event_triggers_matching_workflow, emit_event_with_no_matching_workflows_is_noop, emit_event_triggers_multiple_matching_workflows (both workflows execute independently), subscriber_receives_emitted_events (broadcast channel verification), workflow_event_emitter_trait_works (trait object usage). All 72 workflow engine tests pass, no warnings.

- WP 7.13 Pipeline Validation: implement configurable schema validation per workflow in packages/workflow-engine/src/executor.rs. Add jsonschema dependency to workflow-engine Cargo.toml. Add validate_message() function that validates PipelineMessage payloads against a structural object schema — CDM payloads are validated for serialization integrity, Custom payloads are validated as JSON objects. Validation failures produce WorkflowError::ValidationFailed with code WORKFLOW_004 and Severity::Fatal. Update run_steps() to support three ValidationLevel modes: Strict validates entry message, every intermediate step output, and exit message; Edges validates only entry and exit messages (allowing invalid intermediates); None skips all validation. Update execute_steps() signature to accept ValidationLevel reference for per-step strict validation after successful plugin execution. Add InvalidOutputExecutor test mock that produces non-object payloads at a configurable call index. 5 new tests: strict_validation_catches_invalid_intermediate_output (first step invalid → ValidationFailed at step 0), edges_validation_allows_invalid_intermediate_but_catches_invalid_exit (last step invalid → exit validation fails), edges_validation_allows_invalid_intermediate_output (first step invalid but last valid → passes), none_validation_allows_everything (invalid final output → still passes), strict_validation_passes_with_all_valid_outputs (3 valid steps → all pass). All 67 workflow engine tests pass.

- WP 7.12 Conditional Branching: implement conditional step branching in packages/workflow-engine/src/executor.rs. Add `resolve_field()` method that extracts values from PipelineMessage payloads using dot-notation field paths (e.g., "payload.category", "result.status"), unwrapping the TypedPayload tagged enum serialization. Add `execute_steps()` recursive method (boxed future for async recursion) that processes step lists and handles conditional branching: when a StepDef has a ConditionDef, evaluate the condition field against the current message using serde_json::Value equality, then execute `then_steps` or `else_steps` as a sub-pipeline accordingly. Missing fields resolve to the else branch. Refactor `run_steps()` to delegate to `execute_steps()` for the top-level step list. 4 new tests: conditional_branch_takes_then_path_when_field_matches (category="spam" routes to spam-handler), conditional_branch_takes_else_path_when_field_does_not_match (category="legit" routes to inbox), conditional_branch_supports_dot_notation_field_path (result.status="approved" routes to send-approval), conditional_branch_takes_else_when_field_missing (missing field routes to else branch). All 62 workflow engine tests pass.

- WP 6.12 Plugin Auth Inheritance: add identity propagation tests (packages/auth/src/tests/identity_test.rs) verifying that AuthIdentity survives PipelineMessage serialization round-trips through the plugin execution boundary. 5 tests: auth_identity_survives_pipeline_round_trip (pocket-id identity serialized to auth_context, round-tripped through JSON, deserialized back with user_id/provider/scopes intact), auth_context_none_preserved_when_unauthenticated (None auth_context stays None), auth_identity_preserved_through_plugin_output (metadata.clone() propagation preserves identity), auth_context_not_present_in_serialized_json_when_none (skip_serializing_if annotation verified), api_key_identity_survives_pipeline_round_trip (api-key provider identity round-trip). Register identity_test module in packages/auth/src/tests/mod.rs. All 52 auth tests pass.

- WP 7.11 EngineError Severity Handling: implement severity-based error strategy override in packages/workflow-engine/src/executor.rs. When a plugin returns an error implementing EngineError, the executor now checks severity() before applying the step's declared error strategy. Fatal severity always halts the pipeline regardless of declared strategy. Warning severity logs via tracing::warn! and continues execution, passing through the previous input unchanged. Retryable severity upgrades the default strategy (no explicit on_error) to retry with default max_retries=3, while respecting explicit step strategies (halt stays halt, skip stays skip, retry uses step's config). Update MockPluginError severity from Fatal to Retryable to align test mocks with the new severity hierarchy. Update existing tests: halts_on_step_failure and async_mode_emits_failed_event_on_error use explicit halt strategy; default_strategy_is_halt_when_on_error_is_none renamed to default_strategy_with_retryable_severity_upgrades_to_retry. Add 10 new severity tests: fatal_severity_halts_even_with_skip_strategy, fatal_severity_halts_even_with_retry_strategy, warning_severity_continues_with_halt_strategy, warning_severity_passes_through_input, warning_severity_continues_with_skip_strategy, warning_severity_continues_with_retry_strategy, retryable_severity_upgrades_default_strategy_to_retry, retryable_severity_respects_explicit_halt_strategy, retryable_severity_respects_explicit_skip_strategy, retryable_severity_uses_step_max_retries_when_declared, retryable_severity_with_no_strategy_uses_default_retries. All 58 workflow engine tests pass.

- WP 5.12 Data Export: implement data export utilities in packages/storage-sqlite/src/export.rs. Add tar and flate2 workspace dependencies. export_full_archive() queries all plugin_data and audit_log rows, serializes as JSON arrays, and packages into a .tar.gz archive containing plugin_data.json, audit_log.json, and metadata.json (with export timestamp, record counts, and format version). export_service_data() queries plugin_data for a specific plugin_id, groups by collection, and produces standard-format output files: .ics (iCalendar VCALENDAR/VEVENT) for events, .vcf (vCard 3.0) for contacts, .mbox (mbox format with From_ separators and body escaping) for emails, and .json for all other collections. Both functions log AuditEventType::DataExport audit events. Internal helpers: to_ical_datetime converts RFC 3339 to iCal datetime format, ical_escape/vcf_escape handle special character escaping, render_ical/render_vcf/render_mbox produce standard format output from CDM records. 13 new tests covering: empty database archive structure, archive with data (record counts, metadata), audit logging for both export types, per-service JSON fallback, events as .ics (SUMMARY, DTSTART, DTEND, LOCATION), contacts as .vcf (N, FN, EMAIL, TEL, ORG), emails as .mbox (headers, body, From_ escaping), empty plugin returns no files, multiple collections produce correct file types, datetime conversion, and escape functions. All 79 storage-sqlite tests pass, build clean.

- WP 4.7 Integration Smoke Test: add example plugin (packages/plugin-sdk-rs/examples/hello_plugin.rs) implementing Plugin trait with a "greet" action that echoes the input PipelineMessage, uses register_plugin! macro for WASM entry-point generation, and includes a custom EngineError type for unknown action handling. Add smoke test (packages/plugin-sdk-rs/tests/smoke_test.rs) with 7 tests verifying: plugin metadata (id, display_name, version), action declaration, known action execution (echo returns same correlation_id), unknown action rejection (error code, severity, source_module), PipelineMessage JSON round-trip, PluginInvocation envelope round-trip, and single-dependency ergonomics (all essential types — Action, PipelineMessage, Severity, StorageContext, WasmCapability, and all CDM types — accessible from life-engine-plugin-sdk alone). SDK library compiles for wasm32-wasip1 target. All 84 SDK tests pass (72 unit + 5 reexport + 7 smoke), clippy clean.

- WP ph2:2.11 Schema Versioning Verification Script: add scripts/check-schema-compat.sh shell script that compares canonical JSON Schema files (.odm/doc/schemas/*.schema.json) between a previous git ref and the current working tree to enforce additive-only schema evolution. Accepts a git ref argument (tag, branch, or commit). Breaking changes detected: required fields removed, field types changed, enum values removed, fields removed entirely, collections (schema files) deleted. Allowed changes: new optional fields, new enum values, new collections added, description changes. Uses jq for JSON parsing and git for ref comparison. Exits non-zero with detailed report on breaking changes. Prints summary of all additive changes found.

- WP 9.10 Shutdown Integration Test: add shutdown integration test (apps/core/tests/shutdown_test.rs) that spawns the Core binary as a subprocess with a minimal config (SQLite unencrypted, port 0, local-token auth), sends SIGTERM via libc::kill(), and verifies: (1) process exits with code 0, (2) no ERROR-level log output on stderr, (3) shutdown step log messages appear in correct reverse-startup order (Stopping transports, Unloading plugins, Stopping workflow engine, Shutting down auth, Closing storage), (4) WAL file is properly checkpointed or removed after shutdown. Tests are #[ignore] since they require the compiled binary. Add libc as a workspace dependency (Cargo.toml) and dev-dependency (apps/core/Cargo.toml) for SIGTERM signal delivery.

- WP 9.8 Transport Instantiation: implement Transport trait for all five transport crates (rest, graphql, caldav, carddav, webhook) and wire transport instantiation into Core startup step 9. Each transport crate now has a Transport trait implementation with from_config() constructor that parses transport-specific TOML config, start()/stop() lifecycle methods with structured tracing, and EngineError-implementing error types with error codes (TRANSPORT_REST_001, etc.) and severity classification. Add toml and tracing dependencies to all transport crate Cargo.toml files. Add all five transport crates as Core binary dependencies in apps/core/Cargo.toml. Add instantiate_transports() async function in apps/core/src/main.rs that reads configured transports from the new-architecture TOML config's [transports.*] sections, matches transport names to their crate implementations, instantiates Transport trait objects, calls start() on each (logging errors but continuing with remaining transports if one fails), and stores handles for graceful shutdown. On shutdown, transport handles are stopped in reverse order before plugin unloading. The built-in REST transport (axum router) remains the primary REST transport via legacy config; the Transport trait instances provide the extensibility pattern for the new architecture. All Core and transport crate tests pass, build clean.

- WP 4.5 WASM Build Configuration: configure the plugin SDK crate for wasm32-wasip1 compilation. Disable jsonschema default features (resolve-http, resolve-file) in workspace Cargo.toml — these features pull in reqwest HTTP client and filesystem resolvers that fail to compile on WASM, and are unused by any crate in the project. Create packages/plugin-sdk-rs/.cargo/config.toml as a reference for plugin authors with `[build] target = "wasm32-wasip1"`. Update crate-level doc comment in packages/plugin-sdk-rs/src/lib.rs with WASM build instructions: rustup target add, cargo build --target, and pointer to the reference config. Verified: `cargo build --target wasm32-wasip1 -p life-engine-plugin-sdk` succeeds, all SDK tests pass, all types crate schema validation tests pass (jsonschema core functionality unaffected by disabling resolvers).

- WP 9.7 10-Step Startup Orchestrator: restructure Core binary's main() into a clear 10-step startup sequence with per-step timing and structured logging (apps/core/src/main.rs, apps/core/Cargo.toml). Add new-architecture package dependencies: life-engine-traits, life-engine-storage-sqlite, life-engine-auth, life-engine-workflow-engine, life-engine-plugin-system. Steps: (1) Load configuration — legacy YAML config + new TOML config validation via config::startup, (2) Initialize logging — tracing subscriber with JSON/pretty format and hot-reload handle, (3) Derive database key — Argon2id key derivation with salt management, (4) Initialize storage — SQLCipher-backed storage with schema registry, (5) Initialize auth — OIDC/WebAuthn provider with rate limiting middleware, (6) Create workflow engine — PipelineExecutor with NoOpPluginExecutor placeholder, (7) Load workflows — YAML workflow loading and trigger registry construction, (8) Discover and load plugins — plugin loader with search engine and supporting subsystems, (9) Start active transports — HTTP router with all API routes and TLS support, (10) Wait for shutdown signal — SIGTERM/SIGINT with graceful shutdown. Each step logs step number, name, and duration in milliseconds. Total startup duration logged when step 10 begins. Failures exit with non-zero code via fail_step! macro. Extract build_router() function for HTTP route construction. Add NoOpPluginExecutor struct implementing PluginExecutor trait for workflow engine initialization before plugins are loaded. All existing tests pass, build clean.

- WP 9.6 Storage Backend Initialization: wire encrypted storage initialization into Core startup (apps/core/src/main.rs, apps/core/src/sqlite_storage.rs). Add SqliteStorage::open_with_key() method that accepts a pre-derived 32-byte encryption key, applies it via PRAGMA key as the first statement after opening the database, verifies the key by reading sqlite_master (returns clear error for wrong passphrase or corrupted file), then delegates to from_connection() for WAL mode, foreign keys, and schema init. Replace the TODO(9.6) placeholder in main.rs step 4b with actual encrypted storage init: when a derived key is available, call open_with_key() with clear error messages for decryption failures ("check passphrase") and permission errors ("check file permissions"); when encryption is enabled but no passphrase configured, defer to /api/storage/init endpoint with in-memory database; when unencrypted, open normally. The packages/storage-sqlite SqliteStorage::init() method (implemented in Phase 5) already handles SQLCipher init with PRAGMA key, WAL mode, foreign keys, and schema DDL — verified complete. All Core and storage-sqlite tests pass, build clean.

- WP 9.5 Key Derivation Integration: wire Argon2id key derivation from life-engine-crypto crate into Core startup sequence (apps/core/src/main.rs). Add life-engine-crypto dependency to apps/core/Cargo.toml. Add passphrase field to StorageSettings in config.rs with custom Debug impl that redacts the value. Add resolve_passphrase() method that checks LIFE_ENGINE_STORAGE_PASSPHRASE env var first (takes precedence), then falls back to config file value. During startup step 4a (before storage init): when encryption is enabled and a passphrase is configured, load or generate a 16-byte salt file (salt.bin in data directory), derive a 32-byte encryption key via life_engine_crypto::derive_key(), and log "Database key derived" (never logging the key or passphrase). On first run, generate random salt and save to salt.bin. On subsequent runs, read existing salt. Invalid salt file length (not 16 bytes) causes a startup error. When encryption is enabled but no passphrase is configured, defer to the existing /api/storage/init endpoint flow. The derived key is prepared for WP 9.6 which will pass it to StorageBackend::init(). All existing tests pass, cargo check clean.

- WP 9.3 Config Validation and Section Delegation: implement validate_config() function in config::startup module (apps/core/src/config.rs). Extend ConfigError enum with four new variants: MissingSection { name }, InvalidValue { section, field, message }, ModuleValidationFailed { module, errors }, and ValidationErrors { errors: Vec<ConfigError> }. All variants implement Display with clear formatted messages. validate_config() performs top-level validation: storage section must be a non-empty TOML table (MissingSection error), zero transports produces a tracing::warn but is not an error, logging level must be one of trace/debug/info/warn/error (InvalidValue error), logging format must be json or pretty (InvalidValue error), plugins path must not be empty, workflows path must not be empty. All errors are collected into a Vec before returning — if one error, returns it directly; if multiple, wraps in ValidationErrors variant. Module-level delegation is scaffolded as placeholder comments for future wiring (storage, auth, transport, plugin modules will expose validate() methods). 12 new tests: valid config accepted, empty storage rejected, invalid log level rejected, invalid log format rejected, multiple errors collected (verifies >= 3 errors for empty storage + bad level + bad format), empty plugins path rejected, empty workflows path rejected, zero transports is not an error, Display for MissingSection, Display for InvalidValue, Display for ModuleValidationFailed. All 28 startup config tests pass, build clean.

- WP 9.2 Config Loading with Environment Variable Overrides: implement load_config() function in config::startup module (apps/core/src/config.rs) that loads config.toml with LIFE_ENGINE_* environment variable overrides. Add ConfigError enum with IoError, ParseError, and EnvVarConversion variants (implements Display and std::error::Error). Config file path resolution: explicit path arg > LIFE_ENGINE_CONFIG env var > DEFAULT_CONFIG_PATH. Missing config file returns defaults (not an error). Env var mapping: strip LIFE_ENGINE_ prefix, split on underscores, lowercase segments to form nested TOML keys (e.g. LIFE_ENGINE_STORAGE_PATH -> storage.path, LIFE_ENGINE_TRANSPORTS_REST_PORT -> transports.rest.port). Type-preserving overrides: if the existing TOML value is integer/float/boolean, attempt to parse the env var string as that type before falling back to string. Sensitive value redaction: any TOML key containing "key", "secret", "password", or "token" (case-insensitive) is replaced with "[REDACTED]" before logging. Add Serialize derive to CoreConfig, WorkflowsConfig, PluginsConfig, LoggingConfig structs and Default impl for CoreConfig. Add expand_tilde() helper for resolving ~ in config paths. 11 new tests (all serialized via ENV_LOCK mutex to prevent env var races): load from TOML file, missing file returns defaults, invalid TOML returns ParseError, env var overrides TOML value, env var preserves integer type, env var creates nested keys, LIFE_ENGINE_CONFIG path override, redact_sensitive hides secrets/keys/passwords/tokens, expand_tilde expands home, expand_tilde ignores absolute paths, set_nested_value creates intermediate tables. All 17 startup config tests pass, build clean.

- WP 9.1 Top-Level Configuration Struct: define new TOML-based CoreConfig struct in apps/core/src/config.rs within a config::startup module for the thin Core orchestrator architecture (Phase 9). CoreConfig uses raw toml::Value for storage and auth sections (delegated to owning modules for parsing), HashMap<String, toml::Value> for transports (keyed by transport name — only configured transports are started), and lightweight typed structs for workflows (WorkflowsConfig with path String), plugins (PluginsConfig with path String and per-plugin config HashMap<String, toml::Value>), and logging (LoggingConfig with level and format Strings). Add DEFAULT_CONFIG_PATH constant with platform-specific config file location (macOS: ~/Library/Application Support/, Linux: ~/.config/). All structs derive Debug, Clone, Deserialize. Add toml dependency to apps/core/Cargo.toml. Six tests: deserialize minimal (empty) config with all defaults, deserialize full config with all sections populated, storage and auth default to empty table, DEFAULT_CONFIG_PATH ends with config.toml, only configured transports present, per-plugin config isolation. All 6 new tests pass, all 33 existing config tests pass, build clean.

- WP 8.9 Host Function — Logging: implement capability-free logging host function in packages/plugin-system/src/host_functions/logging.rs. Define PluginLogLevel enum with Trace/Debug/Info/Warn/Error variants and from_str() parser. Define PluginLogEntry struct with level and message fields. Implement LogRateLimiter with per-plugin tracking using Mutex<HashMap<String, RateLimitState>> — enforces max 100 log entries per second per plugin, resets the 1-second window on elapsed time, emits a single tracing::warn when the limit is first exceeded in a window, silently drops subsequent excess entries. Define LoggingHostContext struct holding plugin_id (String) and rate_limiter (Arc<LogRateLimiter>). Implement host_log(): deserializes JSON input with "level" and "message" string fields, parses log level via PluginLogLevel::from_str (rejects unknown levels), checks rate limiter (returns empty {} on drop), emits structured log entry via tracing macros (trace!/debug!/info!/warn!/error!) tagged with plugin_id structured field. No capability check required — all plugins can log. Export logging module from host_functions/mod.rs. Twelve tests: parse all 5 valid log levels, invalid level returns error, log info succeeds without capability, all levels succeed, invalid JSON returns error, missing level field returns error, missing message field returns error, invalid level string returns error, rate limiter allows entries under limit, rate limiter drops entries over limit, rate limiter tracks plugins independently, host_log drops excess entries silently (returns Ok). All 74 plugin-system tests pass, build clean.

- WP 8.8 Host Function — HTTP: implement HTTP outbound request host function in packages/plugin-system/src/host_functions/http.rs. Add reqwest workspace dependency to packages/plugin-system/Cargo.toml. Define HttpHostContext struct holding plugin_id (String), capabilities (ApprovedCapabilities), and client (reqwest::Client for connection pooling). Define HttpRequest (method, url, headers HashMap, optional body) and HttpResponse (status u16, headers HashMap, body String) as serde Serialize/Deserialize structs. Implement host_http_request(): checks HttpOutbound capability (returns CapabilityViolation error if missing), deserializes HttpRequest from JSON input bytes, validates URL scheme (only http:// and https:// allowed — rejects ftp://, file://, etc.), parses HTTP method via reqwest::Method::from_str (rejects invalid methods), builds reqwest request with 30-second timeout, adds custom headers and optional body, executes request, collects response status/headers/body, enforces 10MB response body size limit, serializes HttpResponse back to JSON bytes. Logs outbound requests at debug level with plugin_id, method, URL, and response status (never logs request/response bodies). Export http module from host_functions/mod.rs. Eight tests: request without http:outbound returns capability error with plugin ID, non-HTTP scheme (ftp://) rejected, file:// scheme rejected, invalid HTTP method (with spaces) returns error, invalid JSON input returns execution error, request/response serialization roundtrips preserve all fields, request to unreachable host returns execution error. All 62 plugin-system tests pass, build clean.

- WP 8.7 Host Function — Events: implement events:emit and events:subscribe host functions in packages/plugin-system/src/host_functions/events.rs. Define EventsHostContext struct holding plugin_id (String), capabilities (ApprovedCapabilities), and event_bus (Arc<dyn WorkflowEventEmitter>). Define EmitRequest (event_name, payload) and SubscribeRequest (event_name) as serde Serialize/Deserialize structs for WASM input deserialization. Implement host_events_emit(): checks EventsEmit capability (returns CapabilityViolation error if missing), deserializes EmitRequest from JSON input bytes, delegates to WorkflowEventEmitter::emit() with event name and payload, returns empty JSON object {} on success. Implement host_events_subscribe(): checks EventsSubscribe capability, deserializes SubscribeRequest, emits a "plugin.subscription.registered" meta-event via the event bus containing plugin_id and event_name (declarative registration — actual delivery happens through workflow triggers, not direct callbacks). Add life-engine-workflow-engine dependency to packages/plugin-system/Cargo.toml for WorkflowEventEmitter trait. Export events module from host_functions/mod.rs. Seven tests using MockEventBus (records emit calls as Vec<(String, Value)>): emit succeeds with events:emit capability (verifies event_name and payload passed to bus), subscribe succeeds with events:subscribe capability (verifies registration meta-event), emit without events:emit returns capability error with plugin ID, subscribe without events:subscribe returns capability error with plugin ID, emitted event is received by event bus (verifies payload fields), invalid emit input returns execution error, invalid subscribe input returns execution error. All plugin-system tests pass, build clean.

- WP 8.6 Host Function — Config: implement config:read host function in packages/plugin-system/src/host_functions/config.rs. Define ConfigHostContext struct holding plugin_id (String), capabilities (ApprovedCapabilities), and plugin_config (Option<serde_json::Value>). Implement host_config_read(): checks ConfigRead capability (returns CapabilityViolation error if missing), returns the plugin's own config section as JSON bytes, returns empty JSON object {} when no config section exists. Function is synchronous (no async needed — config is already loaded in memory). Export config module from host_functions/mod.rs. Four tests: read returns plugin-specific config section, read without config:read capability returns CAP error with plugin ID, nonexistent config section returns empty {}, config preserves types (integers, floats, booleans, strings, nested objects, arrays). All 47 plugin-system tests pass, build clean.

- WP 8.5 Host Function — Storage: implement storage read and write host functions in packages/plugin-system/src/host_functions/storage.rs. Create host_functions module directory with mod.rs and storage.rs. Define StorageHostContext struct holding plugin_id (String), capabilities (ApprovedCapabilities), and storage (Arc<dyn StorageBackend>). Implement host_storage_read(): checks StorageRead capability (returns CapabilityViolation error if missing), deserializes StorageQuery from JSON input bytes, overwrites plugin_id to the calling plugin's ID (prevents impersonation), delegates to StorageBackend::execute(), serializes Vec<PipelineMessage> results back to JSON bytes. Implement host_storage_write(): checks StorageWrite capability, deserializes StorageMutation from JSON input, re-scopes plugin_id via scope_mutation() helper (handles Insert/Update/Delete variants), delegates to StorageBackend::mutate(), returns empty JSON object {} on success. Both functions log operations via tracing (debug level with plugin_id and collection/mutation_type). Add async-trait, chrono, uuid as dev-dependencies for test mock. Nine tests using MockStorage (records execute/mutate calls): read succeeds with storage:read capability, write succeeds with storage:write capability, read without storage:read returns capability error, write without storage:write returns capability error, plugin_id is scoped in query (overwritten from "malicious-plugin" to actual caller), plugin_id is scoped in mutation, read results correctly serialized (2 messages round-trip), invalid query input returns execution error, invalid mutation input returns execution error. Export host_storage_read, host_storage_write, StorageHostContext from lib.rs. All 43 plugin-system tests pass, build clean.

- WP 8.4 Extism Runtime Setup: add extism dependency to packages/plugin-system/Cargo.toml and create runtime module in packages/plugin-system/src/runtime.rs. Define PluginInstance struct wrapping extism::Plugin with plugin_id field and Debug impl. Implement load_plugin(wasm_path, plugin_id, host_functions) that reads WASM binary from disk, creates Extism Manifest with 256MB memory limit (4096 WASM pages) and 30-second execution timeout, builds via PluginBuilder with WASI enabled and registered host functions. Implement load_plugin_from_bytes() for in-memory loading (used in tests). PluginInstance methods: id() returns plugin ID, call(function_name, input) invokes WASM export and returns output bytes, function_exists() checks for exported function. Add wat dev-dependency for compiling test WASM modules. Test WAT module implements Extism PDK protocol (imports input_length, input_load_u8, alloc, store_u8, output_set) with a "greet" export that copies input to output. Six tests: loads valid WASM module, call returns correct output, corrupt binary returns WasmLoadFailed error, loads from file on disk, nonexistent file returns error, function_exists check works. Export PluginInstance, load_plugin, load_plugin_from_bytes from lib.rs. All 34 plugin-system tests pass, build clean.

- WP 8.3 Capability Approval Policy: implement capability approval checking in packages/plugin-system/src/capability.rs. Define ApprovedCapabilities struct wrapping HashSet<Capability> with has(), iter(), len(), is_empty() methods. Implement is_first_party() helper that canonicalizes both plugin_path and plugins_dir and checks starts_with relationship. Implement check_capability_approval(manifest, plugin_path, plugins_dir, approved_capabilities) that: (1) collects declared capabilities from manifest, (2) if first-party (plugin_path inside plugins_dir), auto-grants all declared capabilities, (3) if third-party, compares declared capabilities against approved list, (4) returns PluginError::CapabilityViolation listing unapproved capabilities if any declared capability is not approved, (5) returns ApprovedCapabilities with the intersection on success. Export check_capability_approval and ApprovedCapabilities from lib.rs. Six tests: first-party auto-granted all capabilities, third-party with fully approved capabilities passes, third-party with unapproved capability (storage:write not approved) returns error mentioning the capability and plugin ID, third-party with no config entry refuses to load, third-party with empty declared capabilities loads with none, Display/FromStr round-trip for all 6 capability variants. All 28 plugin-system tests pass, build clean.

- WP 8.2 Manifest Parser: implement manifest.toml parser in packages/plugin-system/src/manifest.rs. Define PluginManifest struct with PluginMeta (id, name, version, description, author), ActionDef (description, input_schema, output_schema), CapabilitySet (required Vec<Capability>), and ConfigSchema (serde_json::Value). Implement parse_manifest() function that reads and parses TOML, validates [plugin] section exists with required fields (id, name, version), validates plugin ID format (lowercase letters, digits, hyphens, starting with letter via regex-like check), validates semver version using semver crate, parses actions section into HashMap, parses capabilities by converting strings via Capability::from_str (rejecting unknown capabilities with helpful error listing valid values), and preserves config schema as raw JSON. Add semver workspace dependency. Export all types from lib.rs. 12 test cases: complete manifest with all fields, minimal manifest with only [plugin] section, missing [plugin] section error, missing required fields (id, name, version) each produce ManifestMissingField error, invalid plugin ID formats (uppercase, spaces) rejected, invalid semver rejected, actions extracted with input/output schemas, capabilities parsed as Capability enum values, unknown capability string rejected with valid list, config schema preserved as JSON Value. All 22 plugin-system tests pass, clippy clean.

- WP 7.10 Retry Error Strategy with Fallback: replace the placeholder retry branch in run_steps() in packages/workflow-engine/src/executor.rs with full retry logic. When a step fails and its on_error strategy is Retry: retry the step with exponential backoff delays (1s, 2s, 4s, 8s...) up to max_retries attempts (default 3) using tokio::time::sleep. On each retry, call the same plugin action with the same input PipelineMessage (current_message.clone()). If any retry succeeds, continue the workflow with the successful output. If all retries exhausted: if a fallback StepDef is declared in the ErrorStrategy, execute the fallback step with the original input and continue the pipeline with its output; if no fallback declared, return WorkflowError::RetryExhausted (WORKFLOW_007) with step_index, plugin, action, retry count, and cause message. If fallback also fails, return RetryExhausted with combined error message. Log retry attempts and fallback execution via tracing::warn! and tracing::info!. Five new tests: retry_succeeds_on_second_attempt (fails once then succeeds on first retry), retry_all_fail_without_fallback_halts_pipeline (verifies RetryExhausted error with correct fields and call count), retry_all_fail_with_fallback_executes_fallback (retries fail then fallback succeeds), retry_default_max_retries_is_3 (no explicit max_retries defaults to 3, verifies 4 total calls), retry_continues_pipeline_after_success (retryable step followed by normal step — pipeline completes). New RetryablePluginExecutor test helper that fails for first N calls then succeeds. All 47 workflow-engine tests pass, build clean.

- WP 7.9 Skip Error Strategy: implement skip error handling in run_steps() in packages/workflow-engine/src/executor.rs. When a step fails and its on_error strategy is Skip: log a warning with workflow_id, step_index, plugin, action, and error via tracing::warn!, then continue execution passing the previous step's output (current_message unchanged) as input to the next step — the failed step's output is discarded. Add tracing::warn import. Four new tests: skip_strategy_continues_pipeline_on_failure (step 2 of 3 fails with skip → all 3 steps attempted, pipeline succeeds), skip_strategy_passes_previous_output_to_next_step (step 3 receives step 1's output when step 2 is skipped), skip_strategy_first_step_fails_passes_initial_message (first step fails with skip → second step receives the original initial PipelineMessage), skip_strategy_pipeline_completes_successfully (single step fails with skip → pipeline still completes OK, returned message is the unchanged initial input). All 42 workflow-engine tests pass, build clean.

- WP 7.8 Halt Error Strategy: refactor run_steps() in packages/workflow-engine/src/executor.rs to check each step's on_error strategy when a step fails, instead of always halting unconditionally. When a step fails, resolve the error strategy (step.on_error.strategy or default Halt). For Halt (or no on_error — default): immediately stop the pipeline, do not execute subsequent steps, return WorkflowError::StepHalted with code WORKFLOW_003, Severity::Fatal, including step_index, plugin ID, action name, and underlying error cause. Clone current_message before passing to plugin_executor.execute() so the previous message is preserved for future Skip strategy (WP 7.9). Add ErrorStrategyType import to executor module. Stub Skip and Retry branches to fall through to halt behavior until WPs 7.9 and 7.10 implement them. Five new tests: halt_strategy_stops_pipeline_on_failure (explicit halt strategy on step 2 of 3 — step 3 not called), halt_error_includes_step_index_and_plugin_info (verifies StepHalted fields), halt_error_has_fatal_severity (verifies WORKFLOW_003 code and Fatal severity via EngineError trait), default_strategy_is_halt_when_on_error_is_none (no on_error field behaves identically to explicit halt), halt_on_first_step_skips_all_remaining (first step fails — only 1 call made). All 38 workflow-engine tests pass, build clean.

- WP 7.7 Initial PipelineMessage Construction: implement TriggerContext enum and build_initial_message() function in packages/workflow-engine/src/executor.rs. Define TriggerContext enum with three variants: Endpoint { method, path, body, auth }, Event { name, payload }, Schedule { workflow_id, fired_at } in packages/workflow-engine/src/types.rs. Uses serde_json::Value for auth to keep workflow engine decoupled from auth crate (MessageMetadata.auth_context is already Option<serde_json::Value>). Implement build_initial_message(TriggerContext) -> Result<PipelineMessage, WorkflowError> that: (1) generates a new UUID v4 correlation_id, (2) derives source string from trigger type ("endpoint:POST /email/sync", "event:webhook.email.received", "schedule:sync-email"), (3) sets timestamp to current UTC, (4) for Endpoint: sets payload to TypedPayload::Custom with request body and auth_context from auth value, (5) for Event: sets payload to TypedPayload::Custom with event payload, auth_context None, (6) for Schedule: sets payload to TypedPayload::Custom with empty object {}, auth_context None. Re-export build_initial_message and TriggerContext from crate root lib.rs. Five tests: endpoint trigger with auth (verifies source, auth_context, payload), endpoint trigger without auth, event trigger (verifies source, payload, no auth), schedule trigger (verifies source, empty payload, no auth), unique correlation IDs across messages. All 33 workflow-engine tests pass, build clean.

- WP 7.6 Sync and Async Execution Modes: extend PipelineExecutor in packages/workflow-engine/src/executor.rs to handle ExecutionMode::Sync and ExecutionMode::Async. Sync mode runs all steps sequentially and returns the final PipelineMessage directly — the transport blocks until completion. Async mode generates a UUID job_id, spawns pipeline execution on a tokio background task via tokio::spawn, and returns immediately with a PipelineMessage containing the job_id, workflow_id, and "accepted" status in metadata. When the background task completes, it emits "workflow.completed" or "workflow.failed" events via a pluggable WorkflowEventEmitter trait. Define JobStatus enum (Running, Completed, Failed) stored in Arc<RwLock<HashMap<Uuid, JobStatus>>> for concurrent status queries via job_status(). Define WorkflowEventEmitter trait with async emit() method and NoOpEventEmitter default implementation. Add with_event_emitter() constructor for injecting a real event bus. Refactor execute_workflow() to dispatch to execute_sync() or execute_async() based on workflow mode, with shared run_steps() for the actual step iteration. Re-export JobStatus, NoOpEventEmitter, WorkflowEventEmitter from crate root lib.rs. Seven new tests: async returns job_id immediately (using SlowPluginExecutor with Notify to control timing, verifies Running status before release and Completed after), async emits workflow.completed event (using RecordingEventEmitter), async emits workflow.failed event on step error, sync returns result after all steps (no job_id in output), async preserves correlation_id, plus existing sync tests unchanged. All 28 workflow-engine tests pass, build clean.

- WP 7.5 Sequential Step Executor: implement PipelineExecutor and PluginExecutor trait in packages/workflow-engine/src/executor.rs. Define PluginExecutor async trait with execute(plugin_id, action, input) -> Result<PipelineMessage, Box<dyn EngineError>> as the abstraction boundary for plugin action execution — Phase 8 will provide the concrete implementation. Implement PipelineExecutor struct holding Arc<dyn PluginExecutor>, with new() constructor and async execute_workflow() method that iterates workflow steps sequentially, passing each step's output PipelineMessage as input to the next step. On step failure, returns WorkflowError::StepHalted with step_index, plugin ID, action name, and cause. Logs workflow start (with step count), each step execution (with index, plugin, action), and workflow completion via tracing. Re-export PipelineExecutor and PluginExecutor from crate root lib.rs. Six tests: single step execution, multiple steps execute sequentially (verifies call count and final output), step output becomes next step input (verifies chaining), halt on step failure (verifies subsequent steps not called and error contains correct step_index/plugin/action), correlation_id preserved through pipeline, empty steps returns initial message unchanged. All 23 workflow-engine tests pass, build clean.

- WP 7.3 YAML Workflow Loader: implement load_workflows() in packages/workflow-engine/src/loader.rs. Scans a configured directory for *.yaml and *.yml files sorted alphabetically for deterministic ordering. Parses each file into a top-level WorkflowFile struct containing a workflows HashMap<String, WorkflowDef>. Validates required fields: every workflow must have a non-empty id and at least one step, every step must have non-empty plugin and action fields. Detects duplicate workflow IDs across files (returns DuplicateWorkflowId / WORKFLOW_001 identifying both files). Detects duplicate endpoint triggers across workflows (returns DuplicateEndpoint / WORKFLOW_002). Logs info-level summary of loaded workflow count. Re-exports load_workflows from crate root. Add tempfile as dev-dependency for tests. Ten unit tests: loads single file, loads multiple files, rejects duplicate workflow ID, rejects duplicate endpoint, rejects workflow with no steps, rejects step missing plugin, rejects invalid YAML, rejects nonexistent directory, ignores non-YAML files, multiple workflows in single file. All tests pass, clippy clean.

- WP 7.1 Crate Scaffold: set up the workflow-engine crate with all required dependencies and core type definitions. Update Cargo.toml to add missing dependencies: tracing (structured logging), uuid (correlation IDs), chrono (timestamps), cron (schedule parsing). Implement WorkflowConfig struct in config.rs with path field (directory containing YAML workflow files), serde Serialize/Deserialize derives. Implement WorkflowError enum in error.rs with 10 variants implementing the EngineError trait: DuplicateWorkflowId (WORKFLOW_001), DuplicateEndpoint (WORKFLOW_002), StepHalted (WORKFLOW_003, Fatal), ValidationFailed (WORKFLOW_004, Fatal), LoadFailed (WORKFLOW_005, Fatal), InvalidDefinition (WORKFLOW_006, Fatal), RetryExhausted (WORKFLOW_007, Retryable), EventBusError (WORKFLOW_008, Retryable), SchedulerError (WORKFLOW_009, Retryable), PluginExecutionError (WORKFLOW_010, Retryable). All errors report source_module "workflow-engine". Update lib.rs to re-export WorkflowConfig and WorkflowError as the public API. Crate compiles and tests pass with no warnings.

- WP 6.10 API Key Management: implement full API key CRUD handlers in packages/auth/src/handlers/keys.rs backed by StorageBackend. create_key() generates a cryptographically random 32-byte key (URL-safe base64 encoded), generates a unique 16-byte salt via life_engine_crypto::generate_salt(), hashes the key with HMAC-SHA256 using the salt as the HMAC key, stores the ApiKeyRecord (with hash and salt, never the raw key) in the "credentials" collection via StorageBackend::mutate(Insert), and returns the raw key string (shown to user once) and the record. list_keys() queries all records from storage. revoke_key() finds the record by UUID, sets revoked = true, and writes back via StorageMutation::Update. validate_key() iterates all stored keys, hashes the provided raw key with each key's salt, uses hmac_verify() for constant-time comparison, checks revoked and expired status (returning KeyRevoked for either), updates last_used timestamp on success (best-effort), and returns AuthIdentity with provider "api-key". ApiKeyProvider now requires Arc<dyn StorageBackend> in its constructor — the create_auth_provider() factory function accepts an optional storage parameter (required for "api-key" provider). PipelineMessages for storage use SchemaValidated::new() with a permissive empty schema. Add rand dependency to auth crate. Re-export create_key, list_keys, revoke_key, validate_key from crate root. All 31 existing auth tests pass, full workspace builds clean.

- WP 6.9 Rate Limiting: enhance RateLimiter in packages/auth/src/handlers/rate_limit.rs with cleanup() method and configurable constructor. Add with_config(max_failures, window_secs) constructor for testing with custom thresholds/windows. Add cleanup() method that prunes all IPs whose failures have aged out of the sliding window — prevents unbounded memory growth when called periodically from a background task. Add tracked_ips() diagnostic method returning count of currently tracked IPs. Add comprehensive test suite in packages/auth/src/tests/rate_limit_test.rs: first 4 failures do not trigger rate limit, 5th failure triggers with retry_after > 0, different IPs tracked independently, window expiry clears rate limit (using 1-second window), retry_after decreases as window slides, cleanup removes stale entries, cleanup preserves active entries, no failures means not rate-limited, Default impl matches new(). All 31 auth tests pass, crate compiles clean.

- WP 6.7 Auth Validation Pipeline: implement the transport-agnostic validate_request() function in packages/auth/src/handlers/validate.rs and the RateLimiter struct in packages/auth/src/handlers/rate_limit.rs. validate_request() is the public entry point for all authentication — transports extract the Authorization header and client IP, then call this function. Logic: (1) check if client IP is rate-limited via RateLimiter::is_rate_limited(), returning AuthError::RateLimited with retry_after seconds if so, (2) return AuthError::TokenMissing if no Authorization header, (3) parse the scheme — "Bearer " prefix delegates to provider.validate_token(), "ApiKey " prefix delegates to provider.validate_key(), anything else returns AuthError::TokenInvalid, (4) on any validation failure, record the failure in the rate limiter before returning the error. RateLimiter implements per-IP sliding window tracking: HashMap<String, Vec<Instant>> behind RwLock for concurrent access, 5 failures within 60 seconds triggers rate limiting, is_rate_limited() prunes expired entries and returns retry_after seconds based on oldest failure in window, record_failure() appends current timestamp. Re-export validate_request and RateLimiter from crate root lib.rs. All 14 existing tests pass, crate compiles clean.

- WP 6.6 Pocket ID Provider Tests: add 14 unit tests for PocketIdProvider JWT validation in packages/auth/src/tests/pocket_id_test.rs. Use wiremock to mock OIDC discovery and JWKS endpoints, and a deterministic Ed25519 test keypair to generate JWTs. Tests cover: valid JWT returns correct AuthIdentity with user_id, provider, and scopes; valid JWT with audience claim validates correctly; scope as JSON array parses to Vec<String>; no scope claim returns empty scopes; expired JWT returns AuthError::TokenExpired; invalid signature (different key) returns AuthError::TokenInvalid; wrong issuer returns AuthError::TokenInvalid; wrong audience returns AuthError::TokenInvalid; JWKS refresh when key not initially cached succeeds after fetching keys; unreachable issuer returns AuthError::ProviderUnreachable with Severity::Retryable; malformed token string returns AuthError::TokenInvalid; missing sub claim returns AuthError::TokenInvalid; validate_key delegates to KeyInvalid; revoke_key delegates to KeyInvalid. Add wiremock as workspace and dev dependency. All 14 tests pass.

- WP 6.4 AuthProvider Trait: define the AuthProvider trait in packages/auth/src/lib.rs with three async methods: validate_token() for JWT bearer token validation, validate_key() for API key validation, and revoke_key() for API key revocation by UUID. Add create_auth_provider() factory function that validates the AuthConfig and returns a boxed AuthProvider — dispatching to PocketIdProvider for "pocket-id" or ApiKeyProvider for "api-key" provider types. Implement PocketIdProvider skeleton in handlers/validate.rs with issuer, audience, and jwks_refresh_interval fields (constructor validates issuer is present). Implement ApiKeyProvider skeleton in handlers/keys.rs. Both providers return stub errors for now — full implementations follow in WP 6.5 (JWT validation) and WP 6.10 (API key CRUD). Also marks WP 6.2 (Auth Error Types) and WP 6.3 (Auth Config and Types) as complete since they were already fully implemented during the 6.1 scaffold. Crate compiles and tests pass.

- WP 6.1 Auth Crate Scaffold: set up the life-engine-auth crate with all required dependencies and core type definitions. Update Cargo.toml to add missing workspace dependencies: ed25519-dalek (Ed25519 signature verification), tokio (async runtime), tracing (structured logging), uuid (key identifiers), chrono (timestamps), and tempfile (dev). Implement AuthError enum in error.rs with 8 variants: TokenMissing (AUTH_001), TokenExpired (AUTH_002), TokenInvalid (AUTH_003), ProviderUnreachable (AUTH_004), ConfigInvalid (AUTH_005), RateLimited with retry_after (AUTH_006), KeyRevoked (AUTH_007), KeyInvalid (AUTH_008). All implement the EngineError trait with appropriate Severity (ProviderUnreachable is Retryable, all others Fatal) and source_module "auth". Implement AuthConfig in config.rs with serde Deserialize for TOML: provider (default "pocket-id"), issuer (required for pocket-id), audience, jwks_refresh_interval (default 3600s). Includes validate() method and Default impl. Define core types in types.rs: AuthIdentity (user_id, provider, scopes, authenticated_at), AuthToken enum (Bearer/ApiKey), ApiKeyRecord (id, name, key_hash, salt, scopes, created_at, expires_at, revoked, last_used). Update lib.rs with module-level documentation and pub use re-exports for AuthConfig, AuthError, AuthIdentity, AuthToken, ApiKeyRecord. Existing handler stubs (validate.rs, keys.rs, rate_limit.rs) and test module (tests/mod.rs) remain as placeholders for subsequent WPs. Crate compiles and tests pass.

- WP 5.11 Audit Logging Integration: wire audit logging into credential storage operations in packages/storage-sqlite/src/backend.rs. Add try_log_audit() helper that calls audit::log_event() and warns on failure via tracing::warn — audit logging never blocks or fails a storage operation. Credential reads: after each credential row is parsed and decrypted in execute_query(), log AuditEventType::CredentialAccess with the credential ID and requesting plugin_id. Credential inserts: after successful INSERT, log AuditEventType::CredentialModify with credential ID, plugin_id, and operation "insert". Credential updates: after successful UPDATE (rows_affected > 0), log AuditEventType::CredentialModify with credential ID, plugin_id, and operation "update". Credential deletes: after successful DELETE (rows_affected > 0), log AuditEventType::CredentialModify with credential ID, plugin_id, and operation "delete". Non-credential operations produce no audit entries. Add tracing dependency to storage-sqlite Cargo.toml. Three new integration tests: audit_credential_read_logs_access verifies CredentialAccess entry created on credential query, audit_credential_insert_logs_modify verifies CredentialModify entry created on credential insert, audit_non_credential_operations_do_not_log verifies no audit entries for task operations. All 66 storage-sqlite tests pass, build clean.

- WP 5.10 Audit Logging: implement audit log write functions and retention cleanup in packages/storage-sqlite/src/audit.rs. Define AuditEventType enum with 8 variants: AuthSuccess, AuthFailure, CredentialAccess, CredentialModify, PluginLoad, PluginError, PermissionChange, DataExport. Each variant serializes to snake_case string for storage. Define AuditEvent struct with event_type (AuditEventType), plugin_id (Option<String>), and details (serde_json::Value). Implement log_event() function that generates a UUID v4 for the row id, captures current UTC timestamp in RFC 3339 format, serializes details to JSON string, and inserts a row into the audit_log table. Implement cleanup_old_entries() function that deletes entries with timestamp older than 90 days (using AUDIT_RETENTION_DAYS constant from schema.rs) and returns the count of deleted rows — intended to be called daily by the scheduler. Seven unit tests cover: basic insertion, insertion with plugin_id, JSON details round-trip, unique ID generation across multiple entries, cleanup removes entries older than 90 days, cleanup preserves recent entries, and all 8 event type strings serialize correctly. All 63 storage-sqlite tests pass, build clean.

- WP 5.9 Per-Credential Encryption: implement defence-in-depth per-credential encryption in packages/storage-sqlite/src/credentials.rs using AES-256-GCM on top of SQLCipher database-level encryption. Each credential's claims field is encrypted with a unique key derived via HMAC-SHA256(master_key, credential_id) — using the existing hmac_sign function from packages/crypto. encrypt_credential() extracts the credential id, derives a per-credential key, encrypts the claims JSON with life_engine_crypto::encrypt(), replaces claims with hex-encoded ciphertext, and sets encrypted: true. decrypt_credential() detects the encrypted flag, hex-decodes the ciphertext, derives the same per-credential key, decrypts via life_engine_crypto::decrypt(), restores the original claims JSON, and removes the encrypted flag. Unencrypted credentials (legacy data without the flag) pass through unchanged. Add CredentialEncryption error variant to StorageError (code STORAGE_011, Severity::Fatal) with credential_id and message fields for clear error reporting. Store master_key in SqliteStorage struct (passed from init()) and expose via pub(crate) master_key() accessor. Wire encryption into backend.rs: Insert and Update paths for credentials collection call encrypt_credential() after schema validation but before SQL write. Read path in parse_payload() calls decrypt_credential() before deserializing into Credential struct. Fix two pre-existing clippy collapsible_if warnings in merge_existing_extensions(). Four unit tests in credentials.rs: encrypt/decrypt round-trip preserves claims and removes encrypted flag, wrong key returns CredentialEncryption error, unencrypted credential passes through, different credential IDs produce different ciphertexts. Four integration tests in backend.rs: insert encrypts claims at rest (verified by reading raw data column), read transparently decrypts claims back to original values, wrong key fails decryption, and unencrypted legacy credentials read normally. All 56 storage-sqlite tests pass, clippy clean with -D warnings, full workspace builds.

- WP 5.8 Extensions Object Support: implement extensions merge semantics for canonical collection updates in packages/storage-sqlite/src/backend.rs. Add merge_existing_extensions() helper that, during Update mutations on canonical collections (except credentials), checks if the new payload has null/missing extensions and, if so, reads the existing record's extensions from the database and merges them into the new data. This ensures that updating a record without specifying extensions preserves any existing plugin extension data. When new extensions are explicitly provided, they replace the old ones entirely. The 6 canonical JSON schemas (events, tasks, contacts, notes, emails, files) already include an optional "extensions" property with patternProperties enforcing reverse-domain-name namespacing and additionalProperties: false. The credentials schema intentionally has no extensions field — writes with extensions are rejected. Three new tests: extensions_round_trip_preserves_data verifies write-read preserves extension data, update_without_extensions_preserves_existing verifies merge semantics on update, update_with_new_extensions_replaces verifies explicit extensions replace old ones. All 48 storage-sqlite tests pass, workspace compiles clean.

- WP 5.7 Private Collection Schema Validation: implement PrivateSchemaRegistry and validate_private() in packages/storage-sqlite/src/validation.rs for runtime registration and validation of plugin-declared private collection schemas. PrivateSchemaRegistry stores compiled jsonschema::Validator instances keyed by (plugin_id, collection_name) tuple, enforcing namespace isolation — a plugin can only write to its own private collections. register() method accepts plugin_id, collection_name, and a JSON Schema string; compiles the schema with jsonschema::draft202012::new(); rejects attempts to register canonical collection names (events, tasks, etc.). has_schema() checks if a schema is registered for a given plugin/collection pair. validate_private() looks up the validator by composite key, returns StorageError::UnknownCollection if no schema is registered (preventing writes to undeclared collections), parses data JSON, and validates against the schema returning StorageError::ValidationFailed on first error. Wire validation into backend.rs execute_mutation(): Insert and Update paths now branch on is_canonical() — canonical collections use validate_canonical(), private collections use validate_private() with the SqliteStorage's embedded PrivateSchemaRegistry. Add private_schemas field (PrivateSchemaRegistry) to SqliteStorage struct, initialized as empty in init(). Expose private_schemas_mut() and private_schemas() accessors for plugin manifest registration. Re-export PrivateSchemaRegistry from crate root. Seven new tests: valid private data passes, missing required field fails with ValidationFailed, unregistered collection returns UnknownCollection, cross-plugin write rejected (different plugin_id has no schema), registering canonical name rejected, invalid schema JSON rejected, has_schema returns correct state. All 45 storage-sqlite tests pass, full workspace compiles clean.

- WP 5.6 Canonical Collection Schema Validation: implement schema validation for canonical collection writes in packages/storage-sqlite/src/validation.rs. Embed all 7 canonical JSON Schema files (events, tasks, contacts, notes, emails, files, credentials) at compile time using include_str!. Pre-compile validators into a LazyLock<HashMap> using jsonschema::draft202012::new() for efficient reuse. validate_canonical() function parses the data JSON, looks up the corresponding validator by collection name, and runs validation via iter_errors(). On failure returns StorageError::ValidationFailed (STORAGE_009, Severity::Fatal) with the collection name and the first validation error message. Private collections skip canonical validation (handled separately in WP 5.7). Add ValidationFailed and UnknownCollection error variants to StorageError with corresponding EngineError codes STORAGE_009 and STORAGE_010. Wire validation into execute_mutation() in backend.rs — called after serialize_payload() but before the SQL INSERT or UPDATE executes. Add jsonschema dependency to storage-sqlite Cargo.toml. Seven unit tests cover: valid event passes, missing required field fails with "required" error, event with extensions object passes, credential without extensions passes, credential with extensions rejected (additionalProperties: false), invalid JSON fails, private collection skips validation, and all 7 canonical schemas compile successfully. All 38 storage-sqlite tests pass, full workspace builds and tests clean.

- WP 5.5 Plugin Data Isolation: enforce plugin_id scoping on all storage operations and add capability checks to StorageContext. In packages/storage-sqlite/src/backend.rs, add CANONICAL_COLLECTIONS constant listing the 7 CDM collections (events, tasks, contacts, notes, emails, files, credentials) and is_canonical() helper. Modify execute_query() so canonical collection reads omit the plugin_id WHERE clause (allowing cross-plugin reads of shared data), while private collection reads remain strictly scoped by plugin_id. Write operations (insert, update, delete) are always scoped by plugin_id — a plugin can only modify its own data. In packages/plugin-sdk-rs/src/storage.rs, add capabilities field (HashSet<Capability>) to StorageContext and update constructor to accept it. Add require() method that returns CapabilityViolation (CAP_002, Severity::Fatal) if the requested capability is missing. All read operations (QueryBuilder::execute) check for storage:read capability. All write operations (insert, update, delete) check for storage:write capability. Four new isolation tests in backend.rs: canonical reads span all plugins, private reads scoped by plugin_id, cross-plugin update fails, cross-plugin delete is no-op. Six new capability tests in storage.rs: read with/without storage:read, write with/without storage:write, update/delete without storage:write. All 102 tests pass across both crates.

- WP 5.4 StorageBackend::mutate for SQLite: implement SqliteStorage::execute_mutation() method in packages/storage-sqlite/src/backend.rs that translates StorageMutation variants into parameterized SQL. Insert generates a UUID for the record id, serializes the PipelineMessage payload to JSON for the data column, sets version to 1, and sets created_at/updated_at to current timestamp. Update uses optimistic concurrency via WHERE version = ? clause — if the version has changed since the caller read it, the update affects 0 rows and returns a ConcurrencyConflict error. Update increments version by 1 and sets updated_at to current timestamp. Delete scopes by both id and plugin_id to ensure a plugin can only delete its own data. All mutations use parameterized queries — no string interpolation. Add serialize_payload() helper function that serializes any TypedPayload variant (all 7 CDM types, 7 batch types, and Custom) to JSON string. Add ConcurrencyConflict error variant to StorageError with id and expected_version fields, error code STORAGE_008, Severity::Retryable. Six new unit tests: insert creates record and sets version to 1, update increments version, update with wrong expected_version returns ConcurrencyConflict, delete removes record, delete scoped by plugin_id does not remove another plugin's data. All 26 tests pass (6 new + 20 existing), workspace compiles clean.

- WP 5.3 StorageBackend::execute for SQLite: implement SqliteStorage::execute_query() method in packages/storage-sqlite/src/backend.rs that translates StorageQuery into parameterized SQL. Builds SELECT against plugin_data table with mandatory WHERE plugin_id and collection clauses. Translates QueryFilter operators to SQL via json_extract(): Eq (=), Gte (>=), Lte (<=), NotEq (!=), Contains (LIKE with wildcards). All filter values bound as parameters — no string interpolation. Supports multi-field ORDER BY using json_extract() with ASC/DESC direction. Applies LIMIT capped at 1000 maximum and optional OFFSET for pagination. Deserializes result rows into PipelineMessage: parses JSON data column into CdmType for canonical collections (events, tasks, contacts, notes, emails, files, credentials), or SchemaValidated<Value> for private collections. Constructs MessageMetadata with row ID as correlation_id, source as "storage:{collection}", current timestamp. Add uuid and chrono dependencies to storage-sqlite Cargo.toml. Seven unit tests: returns matching records with correct CdmType, filters by plugin_id, applies Eq filter, applies Contains filter, sorts ascending by field, applies limit+offset pagination, caps limit at 1000 max, and returns empty vec for empty collection. All 20 tests pass (7 new + 13 existing), workspace compiles clean.

- WP 5.2 SQLCipher Database Initialization: implement SqliteStorage struct and init() constructor in packages/storage-sqlite/src/lib.rs. init() takes a toml::Value config (requiring "database_path" string) and a 32-byte encryption key (derived externally via life_engine_crypto::derive_key()). Opens SQLCipher database with rusqlite, sets PRAGMA key using hex-encoded key for SQLCipher, verifies key correctness by querying sqlite_master (detects wrong key with "not a database" error), enables WAL journal mode for concurrent read performance, enables foreign keys, and runs all schema DDL from schema::ALL_DDL to create tables if they don't exist. Error handling distinguishes: DecryptionFailed (wrong key or corrupted file), PermissionDenied (filesystem access), InvalidConfig (missing database_path), and general Database errors. Add EngineError trait implementation on StorageError with structured error codes (STORAGE_001 through STORAGE_007), severity classification (NotFound is Warning, all others Fatal), and source_module "storage-sqlite". Add Debug impl for SqliteStorage (Connection doesn't derive Debug). Add hex and toml dependencies to storage-sqlite, tempfile as dev-dependency. Seven new integration tests: creates new database with correct tables, reopens existing database preserving data, rejects wrong key, rejects missing config, verifies WAL mode, verifies foreign keys enabled, and idempotent schema creation. All 12 tests pass (7 new + 5 existing schema tests), workspace compiles clean.

- WP 5.1 SQLite Schema DDL: define plugin_data and audit_log table DDL constants in packages/storage-sqlite/src/schema.rs. PLUGIN_DATA_DDL creates the universal document store table with columns: id (TEXT PK), plugin_id (TEXT NOT NULL), collection (TEXT NOT NULL), data (TEXT NOT NULL for JSON), version (INTEGER NOT NULL DEFAULT 1 for optimistic concurrency), created_at and updated_at (TEXT NOT NULL, ISO 8601). Two indexes: idx_plugin_collection composite on (plugin_id, collection) for per-plugin queries, idx_collection on collection alone for cross-plugin canonical queries. AUDIT_LOG_DDL creates the append-only security event table with columns: id (TEXT PK), timestamp (TEXT NOT NULL), event_type (TEXT NOT NULL), plugin_id (TEXT nullable), details (TEXT NOT NULL for JSON), created_at (TEXT NOT NULL). Index idx_audit_timestamp on timestamp for time-range queries and 90-day retention cleanup. AUDIT_RETENTION_DAYS constant set to 90. ALL_DDL array for convenient schema initialization. All DDL uses IF NOT EXISTS for idempotent application. Five unit tests verify: table columns match spec, indexes created, idempotent re-application, version defaults to 1 on insert, and plugin_id nullable on audit_log. All tests pass, build clean.

- WP 4.4 Test Utilities: implement test utilities for plugin authors in packages/plugin-sdk-rs/src/test/ behind a `test-utils` feature flag. MockStorageContext provides an in-memory HashMap-backed storage with the same fluent query API as the real StorageContext: query() with where_eq/where_gte/where_lte/where_contains filters, order_by/order_by_desc sorting, limit/offset pagination. Also provides insert/update/delete methods and assertion helpers (assert_inserted, assert_contains, dump). MockMessageBuilder provides a fluent builder for PipelineMessage instances with sensible defaults (auto-generated UUID, current timestamp, "test" source) and convenience constructors for all 7 CDM types: event(), task(), contact(), note(), email(), file(), credential(). Builder supports overrides via with_source(), with_correlation_id(), with_auth(), and schema-validated custom payloads via with_custom(). Test module gated behind #[cfg(any(test, feature = "test-utils"))] so utilities are available to plugin authors who enable the feature but excluded from production WASM builds. Added "test-utils" feature to Cargo.toml. All 66 plugin-sdk tests pass, build clean.

- WP 4.3 Registration Helper Macros: implement register_plugin! declarative macro in packages/plugin-sdk-rs/src/macros.rs for WASM entry-point generation. The macro generates an Extism-compatible extern "C" fn execute() entry point gated behind cfg(target_arch = "wasm32") so it only compiles for WASM targets. Generated code reads input from Extism host memory, deserializes a PluginInvocation envelope (action name + PipelineMessage), instantiates the plugin via Default::default(), calls Plugin::execute(), and serializes the result as a PluginOutput envelope back to Extism output memory. Plugin authors never write unsafe code — the macro handles all FFI details. Define PluginInvocation struct (action + message) and PluginOutput enum (Ok with PipelineMessage, Error with message/severity/code/source_module) as the host-guest communication protocol. Re-export PluginInvocation and PluginOutput from lib.rs. Four unit tests cover: PluginInvocation round-trip serialization, PluginOutput Ok variant with status tag, PluginOutput Error variant with all fields, and PluginOutput Error round-trip. All 46 plugin-sdk tests pass, build clean.

- WP 4.1 Type and Trait Re-exports: add life-engine-traits dependency to plugin-sdk-rs Cargo.toml and re-export all public types from both life-engine-types and life-engine-traits. Re-exports include: all 7 CDM structs and supporting types, PipelineMessage envelope types, StorageQuery/StorageMutation/QueryFilter/FilterOp/SortField/SortDirection storage types, and from traits crate: Plugin trait, Action struct, EngineError trait, Severity enum, StorageBackend trait, Capability (aliased as WasmCapability to avoid conflict with existing SDK Capability), and CapabilityViolation. Update prelude module with commonly used types from both crates. Add compile-only integration test (5 tests) verifying all re-exports are accessible without direct dependencies on types or traits crates. Full workspace compiles cleanly, all 41 plugin-sdk tests pass.

- WP 3.9 Argon2id Key Derivation: implement derive_key and generate_salt functions in packages/crypto/src/kdf.rs. derive_key uses Argon2id with parameters: memory_cost 65536 (64 MB), time_cost 3 iterations, parallelism 4 lanes, 32-byte output length. generate_salt produces a random 16-byte salt via OsRng. Re-export both functions from crypto crate root. Five unit tests cover: deterministic output (same passphrase + salt = same key), different passphrases produce different keys, different salts produce different keys, output is exactly 32 bytes, and generate_salt produces unique values. All 12 crypto crate tests pass, clippy clean with -D warnings.

- WP 3.8 AES-256-GCM Encryption Primitives: implement encrypt and decrypt functions in packages/crypto/src/encryption.rs using AES-256-GCM. encrypt() generates a fresh 12-byte random nonce via OsRng, encrypts with AES-256-GCM, and returns nonce || ciphertext || tag. decrypt() splits the nonce prefix and decrypts, returning an error for truncated or corrupted input. Define CryptoError enum (already existed in error.rs) with EncryptionFailed and DecryptionFailed variants. Re-export encrypt, decrypt, and CryptoError from lib.rs. Seven unit tests cover: round-trip, empty plaintext, nonce uniqueness (different encryptions of same input differ), wrong key rejection, truncated ciphertext rejection, corrupted ciphertext rejection, and output structure validation. All tests pass, clippy clean with -D warnings.

- WP 3.7 Traits Crate Public API and Tests: finalize traits crate public API by adding StorageQuery and StorageMutation re-exports from life-engine-types crate. Add comprehensive unit tests in packages/traits/src/tests/mod.rs covering: Severity display formatting and convenience methods, EngineError trait object safety verification, Capability FromStr/Display round-trip for all 6 variants, CapabilityViolation error codes (CAP_001 load-time, CAP_002 runtime), Action struct serde serialization/deserialization round-trip including skip_serializing_if behavior for None schemas, and compile-time verification that all 12 public re-exports are accessible from the crate root. All 14 tests pass, clippy clean with -D warnings.

- WP 3.6 Capability Types: add packages/traits/src/capability.rs defining Capability enum with six variants (StorageRead, StorageWrite, HttpOutbound, EventsEmit, EventsSubscribe, ConfigRead). Display formats as lowercase colon-separated strings (e.g. "storage:read"), FromStr parses back. CapabilityViolation error struct implements EngineError with CAP_001 for load-time violations and CAP_002 for runtime violations, both Fatal severity, source_module "capability-enforcement". Includes at_load_time boolean to distinguish violation context. Unit tests cover Display/FromStr round-trip for all 6 variants, unknown string rejection, and error field correctness for both violation types. Re-export Capability and CapabilityViolation from traits crate root.

- WP 3.2/3.3 StorageBackend Trait and Storage Types: add packages/types/src/storage.rs defining StorageQuery (collection, plugin_id, filters with QueryFilter/FilterOp, sort with SortField/SortDirection, limit capped at 1000, offset), StorageMutation enum (Insert/Update/Delete variants with optimistic concurrency via expected_version on Update). All types derive Serialize/Deserialize. Re-export all storage types from types crate root. Implement async StorageBackend trait in packages/traits/src/storage.rs with execute (returns Vec<PipelineMessage>), mutate, and init (takes toml::Value config and 32-byte encryption key) methods, using Box<dyn EngineError> for errors. Re-export StorageBackend from traits crate root.

- WP ph2:2.10 Extension Namespace Enforcement: add packages/types/src/extensions.rs with validate_extension_namespace() function and ExtensionError type (NamespaceMismatch variant). On write, validates all top-level keys in the extensions JSON object match the writing plugin's reverse-domain ID, rejecting cross-namespace writes with a clear error. Reads are unfiltered — all namespaces preserved. Null and empty extensions pass validation. Re-export validate_extension_namespace and ExtensionError from types crate root and plugin-sdk-rs. Unit tests cover: valid namespace passes, cross-namespace write rejected, read returns all namespaces, empty extensions passes, null extensions passes, nested extension data preserved.

- WP ph2:2.9 JSON Schema Validation Tests: add packages/types/tests/schema_validation.rs with 22 integration tests validating all 7 canonical collection JSON Schemas against test fixtures using the jsonschema crate. Valid fixtures pass with zero errors, invalid fixtures fail with descriptive error messages. Tests verify extensions field accepted on all collections except Credentials (which rejects it), required fields enforced on all collections, and enum constraints restricted to defined values (TaskStatus, TaskPriority, EventStatus, NoteFormat, CredentialType). Fix valid tasks fixture removing null completed_at value (schema requires string type, not null).

- WP ph2:2.8 Schema Validation Test Fixtures: create packages/test-utils/fixtures/schemas/ with valid/ and invalid/ subdirectories containing JSON test data for all 7 canonical collections (events, tasks, contacts, notes, emails, files, credentials). Valid fixtures contain fully-populated records exercising all fields including nested objects, arrays, enums, and extensions. Invalid fixtures cover missing required fields, wrong types (string instead of boolean, number instead of string), invalid enum values, invalid UUID format, invalid datetime format, additional properties not allowed, invalid checksum pattern, negative size_bytes, and Credentials with extensions field (which it should not have). These fixtures are the canonical test data for schema validation tests in WP 2.9.

- WP ph2:2.6 PipelineMessage Envelope: add PipelineMessage struct with MessageMetadata (correlation_id, source, timestamp, auth_context) and TypedPayload enum (Cdm/Custom) to packages/types/src/pipeline.rs. CdmType provides a discriminated union of all 7 canonical collection types plus batch variants (EventBatch, TaskBatch, etc.). SchemaValidated<T> newtype wrapper guarantees JSON Schema validation via jsonschema crate before values enter the pipeline, with Deref for transparent access. Add jsonschema dependency to types crate. Re-export PipelineMessage, MessageMetadata, TypedPayload, CdmType, SchemaValidated, and SchemaValidationError from lib.rs. Fix pre-existing clippy warnings: collapse nested if in events.rs validate_time_range, use derive(Default) with #[default] attribute on TaskPriority::Medium and TaskStatus::Pending instead of manual impls.

- WP ph2:2.5 Notes, Emails, Files, and Credentials Rust Structs: update all four remaining CDM Rust structs in packages/types to match their JSON Schemas. Notes adds NoteFormat enum (Plain/Markdown/Html) and pinned (Option<bool>). Emails restructured with EmailAddress struct (name/address) for from/to/cc/bcc fields, body_text now Option<String>, adds date (DateTime<Utc>), message_id, in_reply_to for threading, read/starred booleans; EmailAttachment updated to filename/mime_type/size_bytes/content_id (removes file_id). Files renames name to filename, size to size_bytes, checksum becomes required String (not Option), adds storage_backend. Credentials replaces issuer/issued_date/expiry_date with name/service/encrypted/expires_at, removes extensions field. Updated all downstream consumers: GraphQL API types and resolvers (GqlEmailParticipant for email addressing), email connector normalizer and tests, filesystem connector normalizer/local/s3, test-fixtures JSON files, test-utils factory functions and assertion macros, validation tests, schema validation integration tests.

- WP ph2:2.4 Contacts Rust Struct with Nested Types: restructure Contact types in packages/types to match the canonical JSON Schema. ContactName replaces display field with prefix/suffix/middle optional fields. EmailAddress renamed to ContactEmail with typed ContactInfoType enum (Home/Work/Other) instead of free-form string. PhoneNumber renamed to ContactPhone with PhoneType enum (Mobile/Home/Work/Fax/Other) and primary boolean. PostalAddress renamed to ContactAddress with region (was state), postal_code (was postcode), and address_type fields. Contact struct renames organisation to organization, adds title, birthday (NaiveDate), photo_url, notes, groups fields. Updated all consumers across the codebase: plugin-sdk-rs re-exports, test-utils factory, validation tests, api-carddav serializer/protocol (with string-to-enum conversion helpers for vCard parsing), connector-contacts google/normalizer (with type mapping from Google API strings to CDM enums), and GraphQL layer (GqlContactName, GqlPhoneNumber, GqlPostalAddress, GqlContact types and mapping code).

- WP ph2:2.3 Events and Tasks Rust Structs: update CalendarEvent and Task structs in packages/types to match JSON Schemas. CalendarEvent gains structured Recurrence type (frequency, interval, until, count, by_day) with from_rrule/to_rrule helpers, Attendee struct (name, email, status with AttendeeStatus enum), Reminder struct (minutes_before, method), EventStatus enum (confirmed/tentative/cancelled), optional end field, all_day boolean, timezone string. Task gains aligned enums: TaskStatus (Pending/InProgress/Completed/Cancelled) and TaskPriority (Low/Medium/High/Urgent) with Default impls, renamed labels to tags, added completed_at, assignee, parent_id fields. Updated all dependent code across connector-calendar (normalizer, google, caldav), api-caldav (serializer, protocol), GraphQL API, test-utils, test-fixtures, plugin-sdk re-exports, validation tests, schema_registry, and quarantine routes.

- WP ph2:2.2 Contacts, Notes, Emails, Files, Credentials JSON Schemas: update all 5 remaining canonical collection schemas from JSON Schema Draft-07 to Draft 2020-12. Contacts schema restructured with $defs for ContactName (given/family required, prefix/suffix/middle optional), ContactEmail, ContactPhone, ContactAddress nested types, type enums (home/work/other for emails/addresses, mobile/home/work/fax/other for phones), primary boolean fields, renamed organisation→organization, added title/birthday/photo_url/notes/groups fields. Notes schema adds format enum (plain/markdown/html) and pinned boolean. Emails schema restructures from/to/cc/bcc as EmailAddress objects (name optional, address required), adds date/message_id/in_reply_to for threading, updates attachments to EmailAttachment with size_bytes/content_id, adds read/starred booleans. Files schema renames name→filename and size→size_bytes, adds storage_backend field, adds SHA-256 hex pattern validation on checksum. Credentials schema renames type→credential_type with $defs CredentialType enum, replaces issuer/issued_date/expiry_date with name/service/encrypted/expires_at fields, removes extensions field per spec (claims serves that purpose). All schemas now have $id metadata and uuid format on id fields.

- WP ph2:2.1 Events and Tasks JSON Schemas: upgrade both schemas from JSON Schema Draft-07 to Draft 2020-12 with richer structured types. Events schema now has structured recurrence object (frequency, interval, until, count, by_day), typed attendees with response status enum (accepted/declined/tentative/needs-action), reminders with method enum (notification/email), event status enum (confirmed/tentative/cancelled), timezone field, all_day boolean, and end as optional. Tasks schema updated with corrected status enum (pending/in_progress/completed/cancelled), priority enum (low/medium/high/urgent), both now optional fields. Added completed_at, tags, assignee, parent_id for subtask relationships. Both schemas add $id metadata and uuid format on id fields.

- WP 1.11 Community Plugin Build Verification: verify community plugins build independently from the monorepo with only life-engine-plugin-sdk as a dependency. Created temporary test plugin outside monorepo implementing CorePlugin trait with one action. Compiled successfully to wasm32-wasip1 target producing valid 3.3MB WebAssembly module. Verified .wasm file loadable by Extism runtime. Added serde_json re-export to plugin SDK so plugin authors don't need it as a separate dependency. Cleaned up temporary project after verification. Phase 1 now complete.

- WP 1.9 Directory Layout Verification: verify complete directory structure against ARCHITECTURE.md. All 24 required top-level directories confirmed present (apps/core, 15 package crates, 7 plugin crates, tools/templates/plugin, .odm/doc). All 7 plugin crates have complete standard layout (lib.rs, config.rs, error.rs, steps/, transform/, types.rs, tests/). Add missing tests/ directories with .gitkeep to 5 crates (types, plugin-sdk-rs, dav-utils, test-utils, test-fixtures). Package crate deviations documented and justified by crate purpose. Workspace compiles clean.

- WP 1.8 Plugin Scaffold Template: create tools/templates/plugin/ with full WASM-compatible plugin scaffold (Cargo.toml, manifest.toml, src/lib.rs with CorePlugin impl, config.rs, error.rs, steps/, transform/, types.rs, tests/). Uses __NAME__/__ID__ placeholders for sed substitution. Fix justfile new-plugin recipe to use compatible placeholder syntax instead of broken just brace escaping. Verified: scaffolded test plugin compiles and all 7 template tests pass.

- WP 1.4 Package Crate Scaffolding: add standard internal layout to all 10 package crates (traits, crypto, storage-sqlite, auth, workflow-engine, transport-rest, transport-graphql, transport-caldav, transport-carddav, transport-webhook). Each crate now has the convention: lib.rs with module declarations, config.rs, error.rs with thiserror-based error enums, handlers/mod.rs (or domain-specific modules like storage.rs, transport.rs, plugin.rs for traits; encryption.rs, kdf.rs, hmac.rs for crypto; loader.rs, executor.rs, event_bus.rs, scheduler.rs for workflow-engine), types.rs, and tests/mod.rs. Also marks WP 1.3 (pnpm workspace) as complete since it was already configured. All crates compile.

- WP 1.2 Nx Configuration: add project.json files for 10 packages missing Nx configuration (traits, crypto, storage-sqlite, auth, workflow-engine, transport-rest, transport-graphql, transport-caldav, transport-carddav, transport-webhook) with build, test, and lint targets using cargo commands. Update nx.json to add test→build dependency in targetDefaults and include test files in Rust namedInputs. All 27 projects now discovered by Nx.

- WP 1.1 Cargo Workspace Configuration: restructure root Cargo.toml workspace members to match new modular architecture. Add 10 new package crates as minimal shells (traits, crypto, storage-sqlite, auth, workflow-engine, transport-rest, transport-graphql, transport-caldav, transport-carddav, transport-webhook). Organize workspace members into logical groups (core binary, foundation, infrastructure, transports, utilities, plugins). Add toml and cron to workspace.dependencies. All crates compile and pass clippy.

- WP 1.6 Justfile Development Commands: add justfile with dev-core (cargo-watch on apps/core and packages with auto-restart), dev-app (vite dev server for Admin UI), and dev-all (runs both concurrently) recipes for one-command development workflow.

- WP 1.5 Plugin Crate Scaffolding: scaffold all 7 first-party plugin crates (connector-email, connector-calendar, connector-contacts, connector-filesystem, webhook-sender, search-indexer, backup) with WASM-compatible standard layout. Add manifest.toml with [plugin], [actions], [capabilities], and [config] sections to each. Set crate-type = ["cdylib", "rlib"] for WASM compilation. Add config.rs, error.rs, steps/mod.rs, transform/mod.rs, types.rs, and tests/mod.rs to each crate. Create search-indexer plugin from scratch with Tantivy dependency. Add thiserror to 4 plugins missing it. All 478 existing tests pass.

- 10-Phase Migration Plan: create comprehensive migration plan in `.odm/planning/plans/` covering the complete transition from the current monolithic architecture to the new modular crate architecture. 10 phase documents (phase-01 through phase-10) with 135 work packages and 172 detailed tasks ordered by dependency graph: monorepo tooling, canonical data models, infrastructure contracts and crypto, plugin SDK, data layer and storage, authentication, workflow engine, plugin system and capabilities, core binary and startup, deployment/migration/plugin conversion.

- Architecture Redesign: add ARCHITECTURE.md as single source of truth defining four-layer modular architecture (transports, workflow engine, plugins, data layer). Rewrite 5 design docs (Overview, Plugins, Workflow, Data, Transports) and mark ADR-011 as superseded (WASM from day one). Delete 5 obsolete docs (Summary, MonoRepo Tooling, Connectors, API). Remove 7 specs no longer aligned (ci-and-cd, connector-architecture, background-scheduler, rest-api, shell-data-api, sync-layer, plugin-loader). Rewrite 11 remaining specs to reflect thin Core orchestrator, WASM plugins via Extism, declarative YAML workflows, configurable transports, StorageBackend trait, and StorageContext query builder.

- WP 2.8 Config Editor — Auth & Storage: add AuthSettingsForm with provider radio selector (local-token/oidc/webauthn), conditional OIDC fields (issuer_url, client_id, client_secret, jwks_uri, audience) and conditional WebAuthn fields (rp_name, rp_id, rp_origin, challenge_ttl_secs). Add StorageSettingsForm with backend radio (sqlite/postgres), encryption toggle, Argon2 inputs (memory_mb, iterations, parallelism), conditional PostgreSQL fields (host, port, dbname, user, password, pool_size, ssl_mode). Wire both forms into ConfigPage with edit buttons on Authentication and Storage sections.

- WP 2.7 Config Editor — Core & Network: add CoreSettingsForm (host, port, log_level, log_format, data_dir inputs with validation) and NetworkSettingsForm (TLS toggle with cert/key path inputs, CORS allowed_origins list editor with add/remove, rate_limit number input). Wire both forms into ConfigPage with per-section edit/view mode toggling. Section component extended with optional Edit button.

- WP 2.5 App Layout & Routing: add react-router-dom, create Layout shell with Sidebar navigation (Dashboard, Configuration, Plugins, System) with active route highlighting, create placeholder page components (DashboardPage, ConfigPage, PluginsPage, SystemPage), wire up BrowserRouter and Routes in App.tsx/main.tsx

- WP 2.4 Shared Config Types & API Client: add TypeScript interfaces in apps/admin/src/types/config.ts mirroring all CoreConfig Rust structs (CoreSettings, AuthSettings, OidcSettings, WebAuthnSettings, StorageSettings, PostgresSettings, Argon2Settings, PluginSettings, NetworkSettings, TlsSettings, CorsSettings, RateLimitSettings) plus SystemInfo, PluginInfo, and HealthStatus types; add fetch-based API client in apps/admin/src/api/client.ts with getConfig(), updateConfig(), getSystemInfo(), getPlugins(), and healthCheck() methods with ApiError class for error handling

- WP 2.3 Admin App Scaffolding: scaffold React 19 + Vite 6 + TypeScript admin app at apps/admin with Tailwind CSS, PostCSS, dev server proxy to Core on port 3750, Nx project.json with dev/build/preview/lint targets, ESLint config, and .gitignore for build artifacts

- WP 2.1 Config API Endpoints: add GET /api/system/config (returns current CoreConfig with secrets redacted) and PUT /api/system/config (accepts partial JSON, merges with current config, validates, persists to config.yaml). Added CoreConfig.to_redacted_json(), merge_partial(), recursive JSON merge, and Arc<RwLock<CoreConfig>> in AppState. 5 new tests covering redaction, auth requirement, update+persist, validation rejection, and PUT auth.

- WP nx:1.5 Task Pipelines & Developer Scripts: configure composite targetDefaults in nx.json for build-ts, test-ts, type-check, and dev targets with correct dependency ordering; add root package.json scripts (dev, build, build:ts, test, test:ts, lint, type-check, affected:test, affected:build) delegating to nx run-many and nx affected; add dev target to core project.json for cargo run workflow

- WP nx:1.3 Rust Project Configurations: add Nx project.json for apps/core (build/test/lint cargo targets), 4 library packages (plugin-sdk-rs, test-utils, test-fixtures, dav-utils), and 9 engine plugins (connector-email, connector-calendar, connector-contacts, connector-filesystem, api-caldav, api-carddav, webhook-receiver, webhook-sender, backup) with wasm32-wasip1 build targets for plugins. All 15 projects visible in Nx graph.

- WP nx:1.2 Nx Installation & Base Config: install Nx and plugins, generate pnpm lockfile, create nx.json with targetDefaults, cache inputs, and named inputs for Rust and TypeScript

- WP nx:1.1 Root Package & pnpm Workspace: create root package.json with Nx devDependencies and pnpm-workspace.yaml listing apps/*, packages/*, plugins/* workspace members

## 2026-03-22

- WP 1.4 Low Priority Improvements (75 findings across 7 categories): token expiry validation, tilde expansion via directories crate, manifest segment validation, search field indexing, hex decode error handling, semver pre-release support, LWW version check on delete, TOCTOU fix with compare_exchange, CORS wildcard warnings, secret redaction in S3/WebDAV Debug+Serialize, collection param pattern constraint, blanket allow(dead_code) removal, cfg(test) gating, schema quarantine dedup, dead code removal, SSE serialization warning, search depth limit, HashSet collection dedup, bounded delivery log, test retry loops replacing sleep, skip_unless_docker generalization, workspace dependency alignment, license consistency, RFC 5545/6350 line folding, vCard PREF parsing, credential extensions field, user_id/household_id storage columns

- WP 1.3 Medium Priority Fixes (37 tasks): auth middleware cleanup and O(1) token lookup, WebAuthn IDOR and passkey fixes, config secret redaction and validation, main.rs error propagation and TLS connection cap, SQLite PRAGMA key validation, PostgreSQL TLS default-require, per-collection migration verification, household guest write guard, federation lock recovery and sync_history cap, deterministic identity signatures, HashSet conflict merge, federated create_with_id, search STRING field and bulk indexing and BooleanQuery filtering, WASM client reuse and log truncation, wasm_adapter deduplication, dav-utils escape ordering and CRLF handling, credential Debug redaction and HttpMethod enum and NaiveDate dates, streaming SHA-256, RFC 5321 SMTP parsing, sanitized route error responses, typed StorageError enum, federation HTTP tests, GraphQL Playground gating and N+1 batch fix, user_id injection stripping, household route integration tests, connector lock-free IO, backup URL encoding and XML parsing and manifest stats and schedule validation, CalDAV/CardDAV PROPFIND/REPORT TODOs, BackupPlugin unit tests, Dockerfile HEALTHCHECK and dep caching, Docker Compose cleanup, OpenAPI limit maximum, modern launchctl, scaffold input validation

- WP 1.2 High Priority Fixes (26 tasks): proper HMAC-SHA256, HKDF key derivation, SQL injection and LIKE injection prevention, random Argon2 salts, file-backed storage, WebAuthn passphrase fix, async-safe federation locks, URL encoding, X-Forwarded-For rate limiting, WASM HTTP headers/body/allowlist fixes, XML injection escaping in DAV/CalDAV/CardDAV, iCal TZID timezone conversion via chrono-tz, storage init rate-limiting, federation changes validation, GraphQL N+1 fix, credential security headers, household role persistence, backup path traversal prevention, scaffold template path fix, build verification test update

- WP 1.1 Critical Fixes: replace XOR cipher with AES-256-GCM across core crypto, identity, credential store, and backup modules
- Fix SQL injection via unvalidated sort_by field in SQLite and PostgreSQL storage backends
- Replace expect() panic in GraphQL handler with 503 SERVICE_UNAVAILABLE error response
- Update Dockerfile Rust version from 1.83 to 1.85 for edition 2024 support

- Replace phase-based planning (phase-0 through phase-4) with QA-driven fix plan from full-project audit
- Add full-project QA report (196 findings across 170 files) and 76-task fix plan organized by severity
- Disable dependabot (renamed to .disabled)

- Remove committed test RSA private key from repo; generate test keys at runtime using rsa crate
- Add pre-commit hook and CI secret scan to block private key material from future commits
- Add *.pem, *.key, *.p12, *.pfx, *.jks, *.keystore to .gitignore

- Simplify scaffold-plugin to engine-only, remove unused lit/vanilla templates and tests
- Remove JS/TS and Playwright checks from branch protection (Rust-only repo)
- Remove stale QA report and tasks

- Initial project scaffold: Rust workspace with Core backend, engine plugins, plugin SDK, type definitions, docs, CI/CD, and dev tooling

- Add identity credential system with encrypted store, selective disclosure, W3C VC export, and DID support (WP 4.7)
- Implement IdentityStore with separate encryption key for identity documents (passport, licence, certificates)
- Implement selective disclosure: signed time-limited tokens asserting specific claims without revealing raw documents
- Implement disclosure audit log recording what was disclosed, to whom, and when
- Implement W3C Verifiable Credentials 2.0 format export with JSON-LD context and credential subject
- Implement DID alignment using did:key method for future interoperability
- Add REST API routes at /api/identity/credentials for CRUD, /disclose, /audit, /vc, and /api/identity/did
- Extract shared crypto module (derive_key, xor_encrypt, hmac_sha256) from credential_store and identity modules
- Add 59 tests covering CRUD, encryption, log safety, selective disclosure, audit logging, W3C VC format, and DID

- Add encrypted remote backup plugin with full/incremental backup, restore, retention, and scheduling (WP 4.6)
- Implement backup engine with full and incremental backup creation, Argon2id key derivation, and AES-256-GCM encryption
- Implement three storage backends (local filesystem, S3-compatible, WebDAV) via shared BackupBackend trait
- Implement configurable backup scheduling (daily, weekly, custom cron) with next-run computation
- Implement retention policy enforcement to keep only the N most recent backups
- Implement full and partial restore with SHA-256 integrity verification and checksum validation
- Fix backup ID collision by adding millisecond precision to timestamp-based IDs
- Clean up compiler warnings in lib.rs, crypto.rs, and S3 backend
- Add 60 tests covering crypto, engine, backends, retention, and scheduling modules

- Add hub-to-hub federated sync with mTLS encrypted transport and pull-based protocol (WP 4.5)
- Implement FederationStore with peer CRUD, sync cursor tracking, and sync history
- Implement mTLS client builder (reqwest) and mTLS server config (rustls with client cert verification)
- Implement pull-based sync engine: per-collection change pulling, cursor management, last-write-wins conflict resolution
- Implement federation API routes: POST /api/federation/peers, POST /api/federation/sync, GET /api/federation/status, GET /api/federation/changes/{collection}
- Add FederationStore to AppState and wire federation routes into the main router
- Extract shared sync primitives module (sync_primitives.rs) with ChangeRecord, SyncCursors, and apply_change for DRY across federation and Core-to-App sync
- Add federation protocol specification document (docs/federation-protocol.md)
- Add 32 tests covering peer CRUD, selective sync, mTLS validation, status reporting, change application, cursor tracking, sync history, and serialization

- Add GraphQL API with queries, mutations, subscriptions, and playground (WP 4.4)
- Auto-generate GraphQL types from all 7 canonical CDM schemas (tasks, contacts, events, emails, notes, files, credentials)
- Implement typed query resolvers with filtering, sorting, and pagination via GraphQL arguments
- Implement mutation resolvers for create, update (with optimistic concurrency), and delete operations
- Implement subscription resolvers for real-time record change delivery via message bus
- Implement nested query resolution: event attendees to contacts, email attachments to files
- Serve GraphQL Playground at /api/graphql/playground for interactive exploration
- Add async-graphql and async-graphql-axum dependencies to workspace
- Add 20 tests covering type generation, CRUD operations, subscriptions, nested queries, and filtering

- Add multi-user household support (WP 4.3): per-user data isolation, role-based access control, and shared collection management
- Extend AuthIdentity with user_id, household_id, and HouseholdRole (Admin, Member, Guest)
- Add user_id and household_id fields to Record struct for namespace-level data isolation
- Add HouseholdStore with household creation, member invites (with expiry), role management, and shared collection configuration
- Add pure check_record_access function for DRY permission enforcement across storage, routes, and plugins
- Add household REST API routes: create household, invite member, accept invite, update roles, manage shared collections
- Add 20 Rust tests covering data isolation, shared collections, RBAC, invite flows, cross-household denial, and pure permission checking

- Add WASM plugin runtime with Extism integration (WP 4.1): sandboxed WASM execution with capability-scoped host functions and resource limits
- Add WasmHostBridge with 11 host functions (store_read/write/query/delete, config_get, event_subscribe/emit, log_info/warn/error, http_request) enforcing declared capabilities per plugin
- Add collection scoping: WASM plugins can only access collections declared in their manifest
- Add resource limits: 64MB memory default, 30-second execution timeout, configurable rate limiting on host function calls
- Add HTTP domain scoping: outbound requests restricted to declared domains
- Add WasmPluginAdapter for migrating native CorePlugins through the WASM bridge with identical output verification
- Add WASM guest SDK bindings (wasm_guest module) in plugin-sdk-rs with HostRequest/HostResponse types and resource limit constants
- Add 46 tests covering capability enforcement, collection isolation, rate limiting, domain scoping, and migration output equivalence

- Add webhook receiver and sender plugins (WP 3.6): bidirectional webhook support with HMAC-SHA256 signature verification, configurable JSON path payload mapping, and delivery log with status code tracking
- Add webhook receiver plugin with endpoint registration, signature verification, and payload-to-CDM field mapping
- Add webhook sender plugin with event bus subscription matching, exponential backoff retry (max 5 attempts), and delivery history
- Extract shared retry/backoff module into plugin SDK (DRY with email connector sync logic)
- Add 146 tests across webhook-receiver, webhook-sender, plugin-sdk retry, and connector-email crates

- Add CalDAV/CardDAV server API plugins (WP 3.5): expose events as CalDAV calendars and contacts as CardDAV address books for native app connectivity
- Add CalDAV server plugin with PROPFIND, REPORT, GET, PUT, DELETE protocol handlers and iCalendar VEVENT round-trip serialisation
- Add CardDAV server plugin with PROPFIND, REPORT, GET, PUT, DELETE protocol handlers and vCard 4.0 round-trip serialisation
- Add `.well-known/caldav` and `.well-known/carddav` service discovery endpoints (RFC 6764)
- Add DNS SRV records documentation for custom domain deployments
- Add shared DAV XML multi-status builder and vCard helpers to dav-utils package
- Add 86 unit tests covering protocol compliance, serialisation round-trips, and native client (iOS, Thunderbird) compatibility

- Add PostgreSQL storage plugin (WP 3.4): PgStorage adapter with deadpool-postgres connection pooling, JSONB document storage, and full-text search via tsvector/tsquery
- Add atomic SQLite-to-PostgreSQL migration with record count verification, rollback on failure, and progress callbacks
- Add PostgreSQL configuration to CoreConfig with environment variable overrides
- Add complete PG test suite mirroring all SQLite tests (auto-skipped when PG unavailable)

- Add plugin store registry infrastructure (WP 3.1): registry JSON index, CI validation scripts, and PR template for plugin submissions
- Add plugin submission validation with manifest, size, and capability checks (29 unit tests)

- Add `.odm/` project documentation scaffold with specs, planning, and design docs
- Update `.gitignore` with `.playwright*` and `.notes/` patterns
