{
  "name": "plugin-sdk-rs",
  "sourceRoot": "packages/plugin-sdk-rs/src",
  "projectType": "library",
  "tags": ["lang:rust", "type:lib"],
  "namedInputs": {
    "rust": [
      "{projectRoot}/src/**/*.rs",
      "{projectRoot}/Cargo.toml",
      "{workspaceRoot}/Cargo.toml",
      "{workspaceRoot}/Cargo.lock"
    ]
  },
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["rust"],
      "options": {
        "command": "cargo build -p life-engine-plugin-sdk",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "inputs": ["rust"],
      "options": {
        "command": "cargo test -p life-engine-plugin-sdk",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "inputs": ["rust"],
      "options": {
        "command": "cargo clippy -p life-engine-plugin-sdk -- -D warnings",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
