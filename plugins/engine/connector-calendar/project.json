{
  "name": "connector-calendar",
  "sourceRoot": "plugins/engine/connector-calendar/src",
  "projectType": "library",
  "tags": ["lang:rust", "type:plugin"],
  "namedInputs": {
    "rust": [
      "{projectRoot}/src/**/*.rs",
      "{projectRoot}/Cargo.toml",
      "{workspaceRoot}/Cargo.toml",
      "{workspaceRoot}/Cargo.lock"
    ]
  },
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["rust"],
      "options": {
        "command": "cargo build -p connector-calendar --target wasm32-wasip1",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "inputs": ["rust"],
      "options": {
        "command": "cargo test -p connector-calendar",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "inputs": ["rust"],
      "options": {
        "command": "cargo clippy -p connector-calendar -- -D warnings",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
