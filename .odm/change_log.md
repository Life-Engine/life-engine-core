# Change Log

## 2026-03-22

- Initial project scaffold: full monorepo with Core (Rust backend), App (Tauri v2 client), plugin SDKs, first-party plugins, docs, CI/CD, and dev tooling

- Add expenses dashboard widget for shell dashboard (WP 5.18)
- Implement le-expenses-widget custom element with monthly summary (income, expenses, net), top 3 spending categories with percentage bars, miniature doughnut chart, and "View Details" navigation link
- Add computeMonthlyTotals and getTopCategories pure helper functions with 11 unit tests covering aggregation, subcategory grouping, split transactions, and month filtering
- Register widget in plugin.json with id, element, title, and description

- Add subtask hierarchy for todo list plugin (WP 6.3)
- Implement createSubtask with automatic parent project/section inheritance
- Add getSubtasks, getSubtaskProgress (completed/total counter), getIncompleteSubtasks
- Add completeWithSubtasks for cascade completion of parent and pending children
- Add promote to convert subtask to top-level task
- Exclude subtasks from getByProject/getBySection top-level results
- Scope subtask ordering independently from top-level tasks within same parent
- Add 22 unit tests covering subtask creation, progress, cascade, promotion, and filtering (99 total)

- Add mobile releases for iOS and Android (WP 4.8)
- Add bottom navigation bar replacing sidebar on mobile viewports (≤640px) with Home, Pipeline, Store, and Settings items
- Add bottom sheet rendering for shell-modal on mobile with drag handle and slide-up animation
- Add touch-optimised form inputs with 44px minimum tap targets and 16px font to prevent iOS zoom
- Add pull-to-refresh component triggering sync on downward gesture
- Add battery-aware mobile sync scheduler with configurable thresholds and foreground/network resume sync
- Add iOS background fetch and Android WorkManager registration for background sync
- Add push notification manager for sync conflicts, calendar events, and credential expiration warnings
- Configure Tauri v2 iOS build (WKWebView, iOS 15+) and Android build (WebView, API 24+)
- Add tauri-plugin-notification with APNs/FCM support and capability permissions
- Add Playwright E2E test suite for mobile UI: bottom nav, bottom sheets, touch targets, pull-to-refresh, accessibility audit
- Add mobile-shell.page.ts page object for structured mobile test interactions
- Add iOS and Android platform setup scripts (ios-setup.sh, android-setup.sh)

- Add core task CRUD operations for todo list plugin (WP 6.2)
- Implement TaskService on canonical tasks collection: create, read, update, complete, uncomplete, soft delete, restore, move, reorder, duplicate
- Add 5 sort modes: manual, due date, priority, alphabetical, date added
- Tasks use extensions namespace for project/section/order metadata, with validation for title, project_id, and priority
- Wire TaskService into TodosPlugin component
- Add 37 unit tests covering CRUD, status transitions, ordering, movement, sorting, and duplication (77 total)

- Add plugin sidebar and navigation for expense tracker (WP 5.16)
- Add ExpensesSidebar Lit component with net worth display, 7 navigation items (Dashboard, Transactions, Accounts, Budgets, Reports, Recurring, Tags), and per-account balance list
- Replace header tab navigation with sidebar layout in main plugin component
- Add Recurring and Tags views to plugin navigation
- Wire sidebar view-change and account-select events in root component

- Add keyboard shortcuts for expense tracker plugin (WP 5.17)
- Implement KeyboardShortcutService with context-aware conflict prevention (suppresses in inputs, with modifier keys)
- Add CRUD shortcuts: n (new transaction), e (edit selected), Delete/Backspace (delete selected)
- Add list navigation: j/k/arrows with visual selection state on transaction rows
- Add type toggle: 1/2/3 for expense/income/transfer when form is open
- Add view navigation: d (dashboard), b (budgets), r (reports), / (search focus), Escape (dismiss)
- Add search bar UI with inline filtering
- Add E2E test suite for all keyboard shortcut interactions

- Add identity credential system with encrypted store, selective disclosure, W3C VC export, and DID support (WP 4.7)
- Implement IdentityStore with separate encryption key for identity documents (passport, licence, certificates)
- Implement selective disclosure: signed time-limited tokens asserting specific claims without revealing raw documents
- Implement disclosure audit log recording what was disclosed, to whom, and when
- Implement W3C Verifiable Credentials 2.0 format export with JSON-LD context and credential subject
- Implement DID alignment using did:key method for future interoperability
- Add REST API routes at /api/identity/credentials for CRUD, /disclose, /audit, /vc, and /api/identity/did
- Extract shared crypto module (derive_key, xor_encrypt, hmac_sha256) from credential_store and identity modules
- Add 59 tests covering CRUD, encryption, log safety, selective disclosure, audit logging, W3C VC format, and DID

- Add SDK doc validation, platform detection, and release fetcher for the website (WP 1.11)
- Add generate-sdk-docs.mjs script to validate hand-written SDK reference pages cover all public API items
- Add fetch-releases.mjs script to fetch GitHub Releases API data at build time for the downloads page
- Add PlatformDetect Astro component for auto-highlighting the visitor's OS on the downloads page
- Expand Rust SDK reference with CollectionSchema, CredentialStore, StoredCredential, CredentialAccess, collections(), and handle_route()
- Expand JavaScript SDK reference with sync types (Mutation, PushResult, PullResult, SyncAdapter, SyncAdapterConfig, DataStore, ConflictResolver, SyncStatus) and slot types (SidebarItemSlot, MainPageSlot, CommandPaletteSlot, DashboardWidgetSlot, SettingsPageSlot, ContextMenuSlot, SlotType, PluginSlot, SharedModule)
- Add SDK docs validation and website build jobs to CI workflow
- Add @astrojs/rss dependency for RSS feed generation
- Rename downloads.md to downloads.mdx to support Astro component imports

- Add account overview UI with per-account detail view, balance trend chart, and edit/archive actions (WP 5.15)
- Add AccountOverview Lit component with account header (name, type, balance), 30-day balance trend line chart, account-filtered transaction list with pagination, and income/expenses/net summary cards
- Add edit form for account name and type, and archive/unarchive actions dispatched via custom events
- Add Accounts nav tab to main plugin with account list view showing all accounts with computed balances
- Wire account-update and account-archive event handlers in main plugin component

- Add encrypted remote backup plugin with full/incremental backup, restore, retention, and scheduling (WP 4.6)
- Implement backup engine with full and incremental backup creation, Argon2id key derivation, and AES-256-GCM encryption
- Implement three storage backends (local filesystem, S3-compatible, WebDAV) via shared BackupBackend trait
- Implement configurable backup scheduling (daily, weekly, custom cron) with next-run computation
- Implement retention policy enforcement to keep only the N most recent backups
- Implement full and partial restore with SHA-256 integrity verification and checksum validation
- Fix backup ID collision by adding millisecond precision to timestamp-based IDs
- Clean up compiler warnings in lib.rs, crypto.rs, and S3 backend
- Add 60 tests covering crypto, engine, backends, retention, and scheduling modules

- Add reports and analytics UI with 6 chart types, date range presets, filter bar, and view data toggle (WP 5.14)
- Add ReportsView Lit component with tabbed reports: Spending doughnut, Income vs Expenses bar, Category Trends multi-line, Top Expenses ranked list, Account Balance Over Time line, Budget Performance bar
- Add date range selector with 6 presets (this week, this month, last month, this quarter, this year, custom)
- Add filter bar for account, category, and tag filtering across all report types
- Add "View Data" toggle per chart to show underlying numbers as a list
- Extract shared chart-helpers.js with reusable SVG renderers and data aggregation utilities
- Add Playwright E2E tests for reports view interactions
- Integrate reports view into main plugin with Reports nav tab

- Add budget management UI with list, detail, and form views (WP 5.13)
- Add BudgetView component with progress bars, colour-coded percentages, and archived toggle
- Add BudgetDetail slide-in panel with summary cards, budget info, filtered transactions, and SVG historical chart
- Add BudgetForm slide-in panel for create/edit with type toggle, category selector, rollover toggle, and threshold slider
- Integrate budget view into main plugin with Budgets nav tab and full CRUD event wiring
- Add Playwright E2E tests for budget view interactions
- Update ExpensesPage page object with budget view locators and helpers

- Add hub-to-hub federated sync with mTLS encrypted transport and pull-based protocol (WP 4.5)
- Implement FederationStore with peer CRUD, sync cursor tracking, and sync history
- Implement mTLS client builder (reqwest) and mTLS server config (rustls with client cert verification)
- Implement pull-based sync engine: per-collection change pulling, cursor management, last-write-wins conflict resolution
- Implement federation API routes: POST /api/federation/peers, POST /api/federation/sync, GET /api/federation/status, GET /api/federation/changes/{collection}
- Add FederationStore to AppState and wire federation routes into the main router
- Extract shared sync primitives module (sync_primitives.rs) with ChangeRecord, SyncCursors, and apply_change for DRY across federation and Core-to-App sync
- Add federation protocol specification document (docs/federation-protocol.md)
- Add 32 tests covering peer CRUD, selective sync, mTLS validation, status reporting, change application, cursor tracking, sync history, and serialization

- Standardise all 25 specs to the full 4-file structure (brief.md, design.md, requirements.md, tasks.md)
- Convert all spec brief.md and design.md files from YAML frontmatter to HTML comment metadata format
- Add Goals, User Stories, Functional Requirements, and Spec Files sections to all brief.md files
- Create requirements.md for all 25 specs with WHEN/THEN SHALL acceptance criteria
- Create tasks.md for all 25 specs with atomic work packages (1-3 files, 15-30 min scope)

- Add expense dashboard view with summary cards, doughnut chart, budget progress, and sparkline (WP 5.12)
- Add ExpensesDashboard Lit component as default plugin view with six dashboard sections
- Implement balance summary cards: total balance, income this month, expenses this month, net this month
- Implement spending by category doughnut chart with SVG arc rendering and colour-coded legend
- Implement recent transactions list (last 10) with inline quick-add row
- Implement budget progress bars with colour-coded thresholds (green/yellow/orange/red)
- Implement upcoming recurring transactions list (next 5 by due date)
- Implement monthly expense trend sparkline as SVG bar chart (last 6 months)
- Add view navigation (Dashboard / Transactions) to main plugin header
- Wire BudgetService and RecurringService into main plugin for dashboard data loading
- Load primaryCurrency and defaultView from plugin settings
- Update ExpensesPage page object with dashboard locators and view-switching helpers
- Add 11 Playwright E2E tests covering summary cards, doughnut chart, recent transactions, budget progress, upcoming recurring, sparkline, view switching, and quick-add

- Add transaction form UI with type toggle, split items, recurring, and receipt attachment (WP 5.11)
- Add TransactionForm slide-in panel component with create and edit modes
- Implement Expense/Income/Transfer type toggle with colour-coded selection states
- Implement large currency-aware amount input with symbol derived from account currency
- Implement date picker with "Today" shortcut button
- Implement account selector dropdown showing account name and balance
- Implement two-level category selector with parent categories and subcategories, colour dots, and icons
- Implement description input with required validation
- Implement tags input with autocomplete suggestions, chip display, and keyboard entry
- Implement notes textarea with markdown support
- Implement receipt attachment button with file picker and remove action
- Implement split transaction toggle with line-item table, add/remove rows, and total validation
- Implement recurring transaction toggle with frequency selector (daily through yearly)
- Implement exchange rate input for multi-currency transactions with conversion preview
- Implement save/cancel/delete footer with validation error display
- Integrate form into main plugin entry with new transaction button, edit-on-click, receipt and recurring rule creation
- Update ExpensesPage page object with form panel locators and openNewTransactionForm helper
- Add 20 Playwright E2E tests covering form open/close, type switching, field validation, split toggle, recurring toggle, receipt attachment, and edit mode

- Add transaction list view UI with date grouping, quick-add, and shared components (WP 5.10)
- Add TransactionList component with date-grouped rendering, sticky month headers, and inline quick-add row
- Add TransactionRow component with category icon, description, account name, colour-coded amount, tags, split indicator, and running balance
- Extract reusable AmountDisplay, CategoryIcon, and TagChip shared components for cross-view reuse
- Implement pagination with configurable page size (50) and "Load more" button
- Implement running balance column toggled via showRunningBalance plugin setting
- Wire up main plugin entry with TransactionService, real-time data subscription, and quick-add event handling
- Add Playwright E2E page object (ExpensesPage) and 7 E2E test cases for list interactions
- Add 5 unit tests for date grouping, running balance, split detection, and pagination (280 total)

- Add CSV export service with filtered and date-range export (WP 5.9)
- Add CsvExportService with generateCsv, exportFiltered, exportByDateRange, and triggerDownload methods
- CSV columns: date, type, amount, currency, account, category, subcategory, description, tags
- Resolve account and category names from IDs, with parent/subcategory hierarchy support
- Support exporting currently filtered transactions via SearchFilterService integration
- Support date range selection with start/end date validation
- Implement browser file download via Blob URL with configurable filename
- Proper RFC 4180 CSV escaping for commas, double quotes, and newlines
- Tags joined with semicolons within a single CSV field
- Add 15 unit tests covering CSV generation, name resolution, escaping, filtered export, date range export, and download trigger (275 total)

- Add todo list plugin scaffold with project, section, and label CRUD (WP 6.1)
- Bootstrap `com.life-engine.todos` plugin with manifest declaring 9 private collections, 7 settings, 6 commands, and dashboard widget
- Add JSON Schema files for projects, sections, comments, reminders, labels, filters, activity, templates, and stats collections
- Add ProjectService with CRUD, archive/unarchive, reorder, and idempotent Inbox seeding on first load
- Add SectionService with CRUD, rename, collapse/expand, and per-project reorder
- Add LabelService with CRUD, colour management, reorder, and favourite toggle
- Add BaseCrudService helper class with shared getAll, getById, update, delete, reorder, and nextOrder utilities
- Add Lit entry point (`le-todos`) with shell design system integration and Inbox auto-seeding on connectedCallback
- Add 40 unit tests covering project CRUD, section CRUD, label CRUD, default seeding, and edge cases

- Add multi-currency support with formatting, conversion, and primary-currency aggregation (WP 5.7)
- Add CurrencyService with resolveCurrency, toPrimaryCurrency, aggregateInPrimaryCurrency, and getSpendingByCategoryInPrimary methods
- Add per-transaction currency override that defaults from account currency when not specified
- Add manual exchange_rate field on transactions for recording rates at time of entry
- Add conversion to primary currency for report totals using stored exchange rates
- Add convertAmount and getCurrencySymbol helpers to collection-helpers
- Extend transaction schema with exchange_rate property
- Add locale-aware currency symbol display via Intl.NumberFormat
- Add formatWithConversion for dual-currency display (original + converted)
- Add 30 unit tests covering formatting, conversion, override, aggregation, and display (213 total)

- Add receipt attachment service with thumbnail and full-size viewer (WP 5.6)
- Add ReceiptService with attach, getForTransaction, getById, and remove methods via `files` canonical collection
- Implement automatic replacement of existing receipt when attaching a new one (old file deleted)
- Add receipt-thumbnail Lit component with image preview and accessible keyboard/click handling
- Add receipt-viewer Lit component with full-size modal overlay, close on Escape/overlay click
- Add 13 unit tests covering attach, replace, remove, retrieval, validation, and persistence across reload

- Add recurring transaction support with scheduling engine, auto-create and confirmation modes, and catch-up logic (WP 5.5)
- Add RecurringService with CRUD, pause/resume, skip single occurrence, and processAllDue for batch transaction generation
- Add scheduling-engine module with pure functions for next-due calculation across all 6 frequencies (daily, weekly, fortnightly, monthly, quarterly, yearly)
- Add auto-create mode that generates transactions automatically when due, with multi-occurrence catch-up on plugin load
- Add confirmation mode that creates pending transactions requiring user approval
- Add end-date handling that deactivates rules when next_due exceeds end_date
- Add skip functionality to advance next_due without creating a transaction
- Add month-clamping logic for monthly/quarterly/yearly frequencies (e.g. Jan 31 + 1 month = Feb 28)
- Add 42 unit tests covering scheduling engine and recurring service (CRUD, auto-create, confirmation, skip, end-date, catch-up)

- Add GraphQL API with queries, mutations, subscriptions, and playground (WP 4.4)
- Auto-generate GraphQL types from all 7 canonical CDM schemas (tasks, contacts, events, emails, notes, files, credentials)
- Implement typed query resolvers with filtering, sorting, and pagination via GraphQL arguments
- Implement mutation resolvers for create, update (with optimistic concurrency), and delete operations
- Implement subscription resolvers for real-time record change delivery via message bus
- Implement nested query resolution: event attendees to contacts, email attachments to files
- Serve GraphQL Playground at /api/graphql/playground for interactive exploration
- Add async-graphql and async-graphql-axum dependencies to workspace
- Add 20 tests covering type generation, CRUD operations, subscriptions, nested queries, and filtering

- Add budget CRUD with period calculation, spending aggregation, rollover, and alerts (WP 5.4)
- Add BudgetService with create, read, update, archive and validation for category and total budget types
- Add period calculation for weekly, fortnightly, monthly, quarterly, and yearly budget periods
- Add spending aggregation by category (including split transaction attribution) and total expense type
- Add rollover logic carrying unspent amounts forward across periods, capped at zero for overspending
- Add threshold and over-budget alert evaluation with notify:local delivery
- Add budget-engine module extracting pure computation logic (period boundaries, spending, rollover, alerts)
- Add 25 unit tests covering budget CRUD, period calculation, spending tracking, rollover, and alerts

- Add expense tracker plugin scaffold and data layer (WP 5.1): full CRUD services for accounts, categories, and tags with 40 passing tests
- Add plugin.json manifest declaring 6 private collections, capabilities, settings, and Lit shared module dependency
- Add JSON Schema files for transactions, accounts, categories, budgets, recurring rules, and tags collections
- Add AccountService with CRUD, archive/unarchive, reorder, and balance computation (initial_balance + income - expenses + transfers)
- Add CategoryService with hierarchical parent/child CRUD and idempotent default category seeding (11 expense + 6 income categories with subcategories)
- Add TagService with CRUD, colour management, and reorder operations
- Add shared collection helpers with createCollectionWrapper, formatCurrency, and generateId utilities
- Add Lit-based plugin entry point (le-expenses element) with service initialization and automatic default category seeding

- Add multi-user household support (WP 4.3): per-user data isolation, role-based access control, and shared collection management
- Extend AuthIdentity with user_id, household_id, and HouseholdRole (Admin, Member, Guest)
- Add user_id and household_id fields to Record struct for namespace-level data isolation
- Add HouseholdStore with household creation, member invites (with expiry), role management, and shared collection configuration
- Add pure check_record_access function for DRY permission enforcement across storage, routes, and plugins
- Add household REST API routes: create household, invite member, accept invite, update roles, manage shared collections
- Add shell-household web component with member list, invite dialog, role selectors, shared collection management, and activity feed
- Integrate household management section into shell settings page
- Add 20 Rust tests covering data isolation, shared collections, RBAC, invite flows, cross-household denial, and pure permission checking
- Add Playwright E2E test specs and page objects for household management, invite flow, shared collections, and accessibility audit
- Add WASM plugin runtime with Extism integration (WP 4.1): sandboxed WASM execution with capability-scoped host functions and resource limits
- Add WasmHostBridge with 11 host functions (store_read/write/query/delete, config_get, event_subscribe/emit, log_info/warn/error, http_request) enforcing declared capabilities per plugin
- Add collection scoping: WASM plugins can only access collections declared in their manifest
- Add resource limits: 64MB memory default, 30-second execution timeout, configurable rate limiting on host function calls
- Add HTTP domain scoping: outbound requests restricted to declared domains
- Add WasmPluginAdapter for migrating native CorePlugins through the WASM bridge with identical output verification
- Add WASM guest SDK bindings (wasm_guest module) in plugin-sdk-rs with HostRequest/HostResponse types and resource limit constants
- Add 46 tests covering capability enforcement, collection isolation, rate limiting, domain scoping, and migration output equivalence
- Add five first-party app plugins (WP 3.7): Task Manager, Notes, Contacts, Files, and Dashboard forming a complete personal productivity suite
- Add Task Manager plugin with CRUD, list/board views, priorities, labels, due dates, and search filtering
- Add Notes plugin with markdown editor, live preview, tag management, auto-save, and formatting toolbar
- Add Contacts plugin with alphabetical list, search by name/email/org, detail view with communication history, and CRUD form
- Add Files plugin with list/grid views, folder navigation, breadcrumbs, file preview panel, and sort options
- Add Dashboard plugin with overview widgets for upcoming events, recent emails, pending tasks, sync status, and storage usage
- Add Playwright E2E test specs for all five plugins with page objects and accessibility audit
- Extract shared CRUD plugin scaffold with filterByQuery, renderFormModal, renderList, and crudOperation helpers
- Add webhook receiver and sender plugins (WP 3.6): bidirectional webhook support with HMAC-SHA256 signature verification, configurable JSON path payload mapping, and delivery log with status code tracking
- Add webhook receiver plugin with endpoint registration, signature verification, and payload-to-CDM field mapping
- Add webhook sender plugin with event bus subscription matching, exponential backoff retry (max 5 attempts), and delivery history
- Extract shared retry/backoff module into plugin SDK (DRY with email connector sync logic)
- Add 146 tests across webhook-receiver, webhook-sender, plugin-sdk retry, and connector-email crates
- Add CalDAV/CardDAV server API plugins (WP 3.5): expose events as CalDAV calendars and contacts as CardDAV address books for native app connectivity
- Add CalDAV server plugin with PROPFIND, REPORT, GET, PUT, DELETE protocol handlers and iCalendar VEVENT round-trip serialisation
- Add CardDAV server plugin with PROPFIND, REPORT, GET, PUT, DELETE protocol handlers and vCard 4.0 round-trip serialisation
- Add `.well-known/caldav` and `.well-known/carddav` service discovery endpoints (RFC 6764)
- Add DNS SRV records documentation for custom domain deployments
- Add shared DAV XML multi-status builder and vCard helpers to dav-utils package
- Add 86 unit tests covering protocol compliance, serialisation round-trips, and native client (iOS, Thunderbird) compatibility
- Add PostgreSQL storage plugin (WP 3.4): PgStorage adapter with deadpool-postgres connection pooling, JSONB document storage, and full-text search via tsvector/tsquery
- Add atomic SQLite-to-PostgreSQL migration with record count verification, rollback on failure, and progress callbacks
- Add PostgreSQL configuration to CoreConfig with environment variable overrides
- Add storage backend picker to onboarding wizard (SQLite default, PostgreSQL option with connection config)
- Add complete PG test suite mirroring all SQLite tests (auto-skipped when PG unavailable)
- Add first-run onboarding flow (WP 3.3): enhanced wizard with connector selection, OAuth/credential flows, passphrase strength indicator, real-time sync progress, plugin suggestions, and pipeline canvas auto-population
- Add skip option at every interactive onboarding step for power users
- Extract shared `#renderActions` helper and `#getCoreUrl` for DRY wizard rendering
- Add Playwright E2E test specs, accessibility audit, and updated page object for onboarding flow
- Add pipeline canvas visual builder (WP 3.2): SVG node editor with drag-and-drop, connection drawing, config panel, sync status display, and status dashboard
- Extract reusable canvas interaction primitives (coordinate conversion, bezier paths, drag tracking)
- Add Playwright E2E test specs, accessibility audit, and page object for pipeline canvas
- Add Pipeline sidebar item and route to shell
- Add plugin store infrastructure (WP 3.1): registry JSON index, CI validation scripts, store UI component, PR template for plugin submissions
- Add plugin submission validation with manifest, size, and capability checks (29 unit tests)
- Add plugin store Web Component with browse by category, search, detail view, install/uninstall, and capabilities approval flow
- Add Playwright E2E test specs and page object for plugin store
- Add Plugin Store sidebar item and route to shell
- Add `.odm/` project documentation scaffold with specs, planning, and design docs
- Update `.gitignore` with `.playwright*` and `.notes/` patterns
